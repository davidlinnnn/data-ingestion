# Q — bounded fixture-08 acceptance passed

Execution commit `3b54e7d`, identity
`q04-aima-pod-cgroup-20260920-q`. The exact command initially exposed a local
module-path bootstrap error before admission or cluster mutation; commit
`3b54e7d` made the command self-contained. The authorized controlled execution
then ran once, with no automatic retry.

All pre-inference gates passed. AIMA08 fresh, restored and exact replay each
completed 12 pages and nine required OCR components. Each accepted record
verified 615 items, four algorithms, eight continuation edges and the adopted
source-image supplement. All three document digests equal
`dd0115f20c6a173ae9c1a1ebef793696e46df0fdbbe0902f2bf8d68f803ebbfe`;
all full-reference graph digests equal
`2a318c71ca631c277f49fbddb13d08cd07852d55aa4d2cd653c175e87b10e999`.
Temporal histories end in `WORKFLOW_EXECUTION_COMPLETED`, and their decoded
business results independently have `status=complete`,
`processing_complete=true` and 12 registered pages. Replay used the fresh
request id; restored used a distinct request id.

The repaired lifecycle remained in parser PID 131/start tick 16535575 for three
captures and the native assembly restore. The resulting lifecycle status is
`native_capture_restore_same_process_then_reaped`. This directly qualifies the
four-request AIMA path. It does not by itself qualify the original 29-group
cross-document sequence or request-20 recycle.

The sealed `reviewed-window-contract.json` retains P's descriptive phrase
`source-bound reap-before-spawn handoff`. That label is stale for Q and is not
used to infer lifecycle success. The executed measurement contract requires
warm continuity, and the attribution summary proves the same PID across all four
requests. The sealed Q artifact remains unchanged; a future identity should
replace the phrase rather than rewrite this evidence.

All 949 attribution samples are complete. The maximum attribution gap was
0.531139 seconds; the all-sample 4 GiB gate, cgroup PSI and OOM gates passed.
The outer controller recorded 924 samples with maximum gap 0.401278 seconds,
minimum node memory available 6,144,229,376 bytes and maximum cgroup use
1,762,107,392 bytes. No node PSI, cgroup PSI, OOM or deadline stopped the run.
The 4 GiB qualification guard, 5 GiB hard limit, memory floors and
1500/825/300 second deadlines were unchanged.

The durable terminal manifest contains 82 inventory entries. Independent
readback found no mismatch; the 83-file archive matches the sealed evidence
byte-for-byte. Archive SHA-256 is
`3d1da2cf5e2050797244d08cafd64905bb030ff9444229479c08524d1897a640`.
The full 51 MiB local evidence and 23 MiB archive remain at
`/private/tmp/q04-aima-pod-cgroup-20260920-q`; the retained PVC is
`q04-pod-cgroup-q-evidence-20260920-q`, UID
`8f3896bd-c4cb-4c19-b21e-b21ab9a9f2b7`. The compact committed evidence and
`RAW-EVIDENCE-MANIFEST.json` bind all omitted raw files by size and digest.

Owned Deployment, ConfigMaps and Pod were removed with UID preconditions;
persistent channels closed and the evidence PVC remains Bound. A final live
read-only check found no Q Pod, all 32 held Deployments at their exact UIDs and
zero replicas, Temporal healthy and idle, object health 200, node full-memory
PSI avg10 zero and VM OOM count zero.

Disposition: **PASS_BOUNDED_FIXTURE_08_ONLY**. This closes AIMA08 current
fresh/restored/exact-replay, oracle, continuation/four-algorithm, warm
mode-switch and normal resource/telemetry continuity rows. It does not close the
six-fixture matrix, injected telemetry-loss fail-closed guard, original
cross-document request-20 warm sequence, native invalidation, compatible reuse,
changed-profile rejection, or process/Pod interruption and recovery. Q04 and
#51 therefore remain open.
