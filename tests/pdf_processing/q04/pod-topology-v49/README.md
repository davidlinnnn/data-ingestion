# Q04 AX worker-process drain gate

AX repeats the AW process-drain scope once under a new identity with the same frozen AH producer, exact native document digest and unchanged resource guards. AW reached business completion but its harness compared current-v3 output with the historical method-off graph. AX uses the existing source-reviewed current-v3 oracle, which checks the exact graph and source evidence; AW's retained document passes it offline. The old AW runner, failure evidence, PVC and object prefix remain untouched.

The worker process must drain during an active group, generation 2 must recover pages 6–10 on the second Activity attempt, and all 51 pages must pass full consumer and resource qualification. This does not prove Pod drain/recovery. One controlled execution; no automatic retry.
