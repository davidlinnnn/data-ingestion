# AX process drain: node PSI fail-stop after business completion

AX (`q04-process-drain-pod-cgroup-20260925-ax`) ran once with the frozen AH producer, reviewed current-v3 native oracle and unchanged resource guards. Eleven pre-inference gates passed. The owned worker-process drain replaced generation 1 with 2 in the same Pod. Temporal history ended `WORKFLOW_EXECUTION_COMPLETED` at 2026-09-25 01:46:30 UTC; the business result was complete, with 51 registered pages. `drain-proof.json` retained the first group and records exactly one second Activity attempt for pages 6–10. The consumer wrote `checks.json` with the accepted current-v3 exact graph SHA-256 `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`; `document.json` SHA-256 was the frozen `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`. This confirms AW's output difference was harness oracle selection, not a producer regression.

At 01:46:42 UTC, before terminal acceptance, the outer node-memory PSI guard observed `full avg10=0.72` against the unchanged zero limit and stopped the run. The triggering sample is the final record of `vm-controller.jsonl`: 5,385,986,048 bytes available, AX Pod cgroup `full avg10=0`, `memory.events` OOM/kill zero, VM OOM count zero. The read-only host observer shows a concurrent burst of `memory.pressure full` in the same-node `objects-8865c4db6-bqmvr` (MinIO) container cgroup, about 15–22 ms per 0.25-second sample at the peak. Its Pod UID `ee7b24cb-bdb7-408a-9ec8-093d2ab5ac00` matches the observed cgroup path; AX Pod UID was `3726f954-0857-4e09-911c-7303349ef25f` and had no recorded cgroup-full delta in that burst. The object service has a 512 MiB memory limit; a later read showed cumulative `memory.events max=372`, no OOM, and 442,064,896 bytes current. Those cumulative counters do not prove that a limit event occurred in the AX window. The precise object-service operation causing the stall is **unknown**. The evidence supports a real node PSI event centered on the colocated object-service cgroup, not a missing or fabricated controller sample and not AX Pod cgroup pressure.

The outer interrupt prevented `accepted.json` and terminal qualification. Inner `phase-failure.json` records incomplete worker-sample coverage after the stop. Independent attribution summary contains 841 complete process/cgroup samples, but the phase failed and the durable terminal manifest is `INCOMPLETE`; all 55 retained inventory file sizes and hashes match. **No process-recovery matrix row is promoted.** The supervisor and transport stopped, UID-fenced Pod/Deployment/ConfigMap cleanup completed, the 32 held Deployments were independently checked exact/off, and the AX evidence PVC remains Bound at UID `2fda0350-71c9-4929-818c-46789565778c`. Raw evidence and object prefix remain at `/private/tmp/q04-process-drain-pod-cgroup-20260925-ax/` and `q04/process-drain-pod-cgroup-20260925-ax/`. No automatic retry occurred.

One later read-only control fetched AX's immutable 22,539,322-byte `document.json` over a temporary port-forward and verified SHA-256. It took 0.127 seconds and left the object-service cgroup's cumulative `memory.events max=372`, OOM=0 and PSI full total=3,418,637 unchanged; the port-forward was stopped. A single warm-cache fetch therefore did not reproduce the pressure burst. It does not rule out request concurrency, cache state or background object-service work during AX.

Next, investigate the object-service memory-pressure burst under the existing 512 MiB limit and choose a reviewed operating configuration that keeps the node guard meaningful. Do not waive the PSI gate or infer capacity from free memory alone. Any next runtime needs a new identity and one controlled execution. Actual Pod drain/recovery and the #44 operating-bounds handoff remain open; #51 cannot yet be integrated or closed.
