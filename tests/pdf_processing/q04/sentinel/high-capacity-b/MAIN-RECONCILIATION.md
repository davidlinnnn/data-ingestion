# Main reconciliation and remaining Q04 schedule

Reviewed candidate `d543af2`. Bounded Keynote sentinel accepted; #51 stays open.
Recomputed seven private artifact hashes plus the committed executed-helper hash;
all eight summary bindings match. Opened the immutable capture without extracting
or modifying source artifacts. All three actual accepted/result records complete,
producer hashes match current production, document hashes and consumer checks
agree, fresh/restored/replay reuse matches the contract, exact replay retains the
fresh request and complete identity. All three worker records prove parser/scratch
absence with no sampler errors; phase cleanup has no errors. This is evidence
review, not a new runtime or independent visual review of every source region.

The recorded resource/cleanup verdict supports only this sentinel. The 32 paused
historical Deployments must stay at zero; future cleanup must not restore them.
No new execution window, deployment change or publication is authorized here.

## Proposed sequence

Each row requires a separately bounded capacity window, immutable phase name,
current admission and preserved failure evidence. No automatic retry or relaxation.

1. ACL (`09`, original pages 2–4): fresh/restored/replay. Verify six equations,
   TextItem equation 2, exact typed graph and source evidence. Proposed 20 minutes,
   last five for cleanup. This is the next runtime request.
2. YOLO (`07`, 15 pages), then WikiSkill (`06`, 28 pages): separate windows with
   fresh/restored/replay, full tables/captions and required OCR. Choose window and
   per-case timeouts from retained timings before requesting approval; Keynote's
   180-second workflow timeout is not a validated bound for these documents.
3. AIMA (`08`, original 99–110): retain Q03 bounded production acceptance; obtain
   Q04 consumer/fresh-index binding for warm comparison. Reuse only if this run's
   request/profile/source/config contracts actually match. Do not transplant Q03
   registrations or an old index merely because production hashes match.
4. Native 51-page fixture: fresh/restored/replay plus evidence-only variation,
   real method invalidation and old-request protection. Budget separately.
5. With all required fresh-index entries: fixed serial warm sequence, then
   telemetry-loss guard and process recovery in distinct windows. Pod-mode drain
   still needs the previously documented controller/shared-storage/owned-Pod setup;
   process evidence cannot stand in for Pod-loss qualification.

Continue the successful outer 4.5 GiB/60-second/zero-PSI precondition and unchanged
active guards as proposed stop rules. They are not general production sizing.
Before each new window check source, method/models/packages, producer, harness,
versioned storage, exclusive reservation and remaining deadline. Larger fixtures
may fail the current envelope; stop and report instead of increasing limits live.

Prefer the existing frozen Q04 state/bundle and source namespace for subsequent
unique matrix phases: the adapter supports fresh-index merging within one run.
First verify retained state remains intact and matches the bundle. Do not rerun
init on existing paths or mutate frozen configuration to grant a new lease; pass
a new approved capacity file. If state must be rebuilt, document the identity
break and satisfy the new-run baseline requirements before warm/recovery.

Any full-graph delta fails qualification pending source review; fresh/restored
agreement cannot approve a new oracle. Finish #51 only after all required rows and
operating bounds are reviewed. This plan does not close #44/#45/#46.
