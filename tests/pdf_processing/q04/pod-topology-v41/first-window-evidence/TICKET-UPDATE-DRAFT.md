Q04 AP used a new identity and prefix for one controlled required-relationship attempt under unchanged guards. All 11 pre-inference gates passed, but the node memory PSI guard stopped the run before any Temporal workflow or inference began (`full avg10=0.18` versus zero). The retained triggering sample had no Pod or VM OOM kill. A continuous independent probe recorded 20,218 microseconds of node full stall shortly before the stop, with no full-stall increment among its 68 visible cgroups. The pressure source/accounting scope remains unknown; this does not justify relaxing the guard or changing the cluster.

Fail-stop cleanup and independent verification passed: the 26 terminal evidence files matched their sizes and hashes, worker/parser/scratch and owned Kubernetes runtime were removed, the AP PVC remains Bound, and all 32 held Deployments remain exact/off. A read-only follow-up confirmed kind worker2 uses a private cgroup namespace, so its 68 visible cgroups do not cover every possible source of global PSI. A temporary, network-disabled read-only host-cgroup probe exposed 153 cgroups, matched global and root PSI totals, and completed 19 continuous idle samples. No pressure event occurred during that five-second validation, so AP's historical source remains unknown. The relationship interruption/recovery/replay and later Q04 gates remain unaccepted. Next: review this host observer's owned lifecycle before a new-identity controlled runtime, with unchanged guards. #51 remains open; no integration or closure claim.
