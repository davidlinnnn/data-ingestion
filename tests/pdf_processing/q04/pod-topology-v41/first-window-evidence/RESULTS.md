# AP first window: node PSI stopped before workflow start

One controlled run of `q04-relationship-pod-cgroup-20260925-ap` used source commit `2a3760f`, prefix `q04/relationship-pod-cgroup-20260925-ap/`, and the unchanged zero node-memory-PSI guard. All 11 pre-inference gates passed. No workflow, Activity, parser request, inference, or object write started. The required-relationship interruption, recovery, and replay remain unaccepted.

At 2026-09-24 16:46:22.445 UTC the controller stopped on node memory `full avg10=0.18` (limit 0). Its retained triggering sample showed 7,067,918,336 available node bytes, Pod memory 314,884,096 of 5,368,709,120 bytes, Pod `oom_kill=0`, and VM OOM kills 0. The Pod memory PSI `full` remained zero. A separate 250 ms probe was continuous from before admission through cleanup: 715 samples, largest interval 0.268 s. In the ten seconds before to one second after the stop, it recorded 20,218 microseconds of node `full` stall in one sample about 1.085 s before the stop, and zero `full` delta in every one of the 68 cgroups visible to the probe. Thus the node guard measured pressure, but the responsible process or accounting scope is unknown. Visible-cgroup absence does not prove a false alarm, sufficient capacity, or a reason to change the guard or cluster.

The controller stopped the owned supervisor about 0.102 s after its stop record. The supervisor logged `KeyboardInterrupt`; the worker's `VM PSI pressure` failure is an interruption consequence, not an independent Activity failure. There is no Temporal workflow result to classify as success or failure in AP. The 26-file terminal inventory has `INCOMPLETE` status, as required for a stopped run, and every listed byte count and SHA-256 matched independent readback. Worker, parser, scratch, Pod and owned Deployment/ConfigMaps are absent. The AP evidence PVC remains Bound with its original UID; all 32 existing held Deployments retain exact UIDs and zero desired/ready replicas. The external probe was PID/start-tick-fenced, stopped, and independently absent. Raw controller and probe evidence remain under `/private/tmp/q04-relationship-pod-cgroup-20260925-ap*` and on the retained PVC. [Independent verification](INDEPENDENT-VERIFICATION.json).

Read-only follow-up confirmed that kind worker2 runs with Docker `CgroupnsMode=private`. Inside the container, both PID 1 and the diagnostic process report `0::/init.scope`, while cgroup2 is mounted at `/sys/fs/cgroup` from `/`. At follow-up, `/proc/pressure/memory` had `full total=2,616,044` microseconds and the visible cgroup root had `full total=2,033,693`; these cumulative values are from different scopes and cannot themselves attribute AP's event. The private namespace explains why the probe's 68 cgroups were an incomplete attribution universe. It does not establish which external cgroup, host service, or kernel path stalled.

A temporary network-disabled, read-only container using the locally cached kind image and `--cgroupns=host` then exposed 153 cgroups. Its root `memory.pressure` and `/proc/pressure/memory` both read `full total=2,617,181` in the same process; kind worker2 mapped to `/docker/0d08ea948508551ee2177390d1fbb0165aa65de2479ce4d049f0815aed04d91d`. A five-second host-scope idle probe retained 19 continuous samples (largest interval 0.269 s), 153 cgroups at both endpoints, and no node full-stall event. The temporary container removed itself. This validates a wider observation method, not historical AP attribution or a pressure-free future runtime. Raw idle probe: `/private/tmp/q04-host-cgroup-idle-20260925.jsonl`.

Do not retry AP. A later new-identity acceptance attempt can pair the unchanged guard with this host-scope observer, provided its lifecycle and terminal evidence are independently reviewed first. The observer must record host-cgroup deltas through any stop, including cgroups outside kind worker2; if node pressure recurs without a matching host-cgroup delta, retain the unknown classification. Keep the current limit. #51 remains open and is not ready for integration or closure.
