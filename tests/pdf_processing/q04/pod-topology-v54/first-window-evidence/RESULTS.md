# BC: inner process recovery passed; terminal mirror stopped early

BC ran once with the frozen AH bundle and producer, distinct projected worker/host readiness fix, reviewed current-v3 native oracle, unchanged Q04 guards and temporary 768 MiB MinIO limit. Eleven pre-inference gates passed. Both worker generations recorded their first telemetry sample before publishing Ready (240 and 307 microseconds, respectively). The owned process drain retained the first group and restarted pages 6–10 on Activity attempt 2. Temporal business result completed 51/51 pages and seven components; the document SHA-256 was the accepted `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`, and the exact current-v3 graph SHA-256 was `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`. The inner `phase-complete.json` and 838-sample process/cgroup attribution summary both passed with no hard incomplete sample.

The unchanged outer node PSI guard did not fire. The 1,402-sample host observer had a maximum interval of 0.283 seconds. The exact MinIO cgroup under the 768 MiB trial reached 805,036,032 bytes (near the 805,306,368-byte limit), but recorded no new `memory.events max` or OOM and 6,845 microseconds of full PSI. Node-full PSI accumulated 16,870 microseconds, with no interval at or above 10 ms. This is another successful pressure observation under the trial setting, not proof of a permanent operating bound.

The Pod-local durable terminal manifest was `PASS_CANDIDATE`, and all 50 exported inventory files independently match their sizes and SHA-256 hashes. The controller's local mirror had copied only 12,582,912 of `document.json`'s 22,539,322 bytes. Its final drain loop stopped as soon as the smaller `FINAL_REQUIRED` set was complete, then correctly rejected the incomplete inventory member. The phase therefore failed before live terminal qualification and **process recovery is not yet accepted**. This is a controller transport completion bug, not a business-result or parser failure.

The read-only host observer exited and was removed with complete coverage. Owned supervisor, transport, Pod, Deployment and ConfigMaps were cleaned with UID preconditions; BC PVC remains Bound at UID `a5b935d0-c9c6-47d5-a819-05ad8d9fdf65`. Raw local evidence and object prefix `q04/process-drain-pod-cgroup-20260925-bc/` remain retained. No automatic retry occurred. A new run must drain every terminal-manifest inventory member before calling the existing exact-hash finalizer; all acceptance thresholds stay unchanged.
