# #51 update draft — not published

Q04 acceptance harness fixes preserve rejecting telemetry before validation and
filter cleanup worker directories; real failure regressions pass. Historical
runners, samples, PVCs and object prefixes remain unchanged.

Latest controlled execution P (`ed393ad`) ran on existing kind cluster with all
32 held Deployments unchanged at zero. All 11 startup gates passed. AIMA08 fresh,
restored and exact replay each completed 12 registered pages and nine OCR
components; 615-item oracle, four algorithms, eight continuation edges, source
pixel images and equal full graph passed. All three Temporal executions have
business `complete`, not merely terminal COMPLETED.

P still FAILS final qualification: one of 961 process samples changed membership
between enumerations. Its PSS remains unknown; the existing confirmed-exit rule
does not authorize accepting a birth/unknown membership transition. No final
fresh index was promoted. Node PSI/OOM/deadline did not cause P's stop. The final
qualification error caused exit 1 and normal owned cleanup. H's exact trigger
row remains unavailable; L's observed PSI/THP association is not proof of cause.

Local P validation: 141 focused tests and two-axis review passed before runtime.
This is not a claim that the entire historical test suite passes (known missing
fixtures/dependencies and platform-sensitive failures remain documented).
Independent post-run verification: 78 sealed entries, 79 archive files and
inventory digest match; owned Pod/processes/scratch removed, channels closed,
Temporal idle, historical PVC UIDs unchanged. P PVC and raw evidence retained.

Accepted scope remains M's bounded07 and earlier exact09/10 identities. P adds
three-mode AIMA content evidence, but not integrated resource acceptance.
Native/Wiki, current-producer applicability, original 29-group cross-document
warm/request20 recycle, invalidation/reuse and interruption/drain gates remain
open. No Q04 PASS, merge recommendation or #51 closure is justified.

Next implementation is the reproduced lifecycle defect: assembly currently reaps
the warm parser before the original20 capture requests. A no-inference prototype
preserves the original29-group/two-PID contract, but real parser mode-switch,
resource behavior and cancellation still require validation. Improve persisted
before/after identity evidence without relabeling unknown samples. No automatic
runtime retry; review the corrected source and impact before a new identity.

Evidence: tests/pdf_processing/q04/pod-topology-v15/first-window-evidence/RESULTS.md
Matrix: tests/pdf_processing/q04/CURRENT-ACCEPTANCE-MATRIX.md
