# AY process drain: object-service pressure coincides with node PSI stop

AY (`q04-process-drain-pod-cgroup-20260925-ay`) ran once on the existing kind context with the frozen AH producer, current-v3 native oracle, unchanged zero node PSI guard and unchanged deadlines. Eleven pre-inference gates passed. The owned worker process changed from generation 1 to 2 in the same Pod. Temporal history ended `WORKFLOW_EXECUTION_COMPLETED` at 2026-09-25 04:10:43 UTC. Its decoded business result was `complete`, `processing_complete=true`, 51 registered pages and seven components. The retained document SHA-256 was the accepted current-v3 `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`. The retained graph SHA-256 was the accepted current-v3 `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`. The historical method-off `graph-delta.json` reports a difference from `89bdb...`; it is not the reviewed current-v3 oracle and is not acceptance evidence.

At 04:10:53 UTC, before terminal qualification, the outer node PSI guard stopped the run. The exact object-service Pod UID was `ee7b24cb-bdb7-408a-9ec8-093d2ab5ac00`; its container cgroup path and 512 MiB limit remained identical throughout the host observation. Across roughly one second around the stop, node-full PSI accumulated 98,445 microseconds, while this MinIO cgroup's full PSI accumulated 61,100 microseconds and `memory.events max` increased by 27 (419 to 446). Its measured `memory.current` was 536,489,984 bytes at the sample spanning the stop, against `memory.max=536,870,912`; OOM and OOM-kill stayed zero. The simultaneous counters strengthen the attribution of node pressure to the colocated object service. They do **not** identify the precise MinIO request, allocator action or sole cause of node pressure. Available node memory alone cannot invalidate a PSI stall.

The supervisor interrupted the workload after the guard fired; the inner phase recorded `CancelledError`. The retained result file and completed Temporal history show business completion before this interruption, but `accepted.json` and terminal qualification were not produced. The durable terminal manifest is `INCOMPLETE`; all 50 retained inventory entries independently match their file sizes and SHA-256 hashes, but this does not make the run accepted. No process-recovery row is promoted. The read-only host observer was stopped and removed with exit code zero. Its terminal validation rejected a 28.676-second sample interval during post-stop cleanup, so its full-window coverage is incomplete. The synchronized samples at the guard trigger remain retained; this coverage gap must not be called a clean terminal trace.

Owned supervisor and transport stopped; UID-fenced Pod, Deployment and ConfigMap cleanup completed. The AY evidence PVC remains Bound at UID `51424eb4-7257-409d-bf94-bc3339356825`. The 32 held Deployments were rechecked exact/off. Raw local evidence is `/private/tmp/q04-process-drain-pod-cgroup-20260925-ay/`, the host trace is the adjacent `-node-pressure.jsonl`, and the object prefix `q04/process-drain-pod-cgroup-20260925-ay/` is retained. No automatic retry occurred.

Next, investigate why the existing 512 MiB object service reaches its memory limit during the acceptance window, including operation-level timing and supported resource settings. Do not relax the zero node PSI guard merely to pass. A future controlled runtime requires a new identity and reviewed configuration. Actual Pod drain/recovery and the #44 operating-bounds handoff remain open; #51 is not ready for integration or closure.
