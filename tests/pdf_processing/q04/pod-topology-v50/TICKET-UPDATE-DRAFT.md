AY (`q04-process-drain-pod-cgroup-20260925-ay`) executed once with the frozen AH producer, corrected current-v3 oracle, unchanged resource guards/deadlines and a read-only exact MinIO cgroup observer. All 11 pre-inference gates passed. Owned worker generation 1→2 occurred in the same Pod; Temporal completed with a successful business result (51/51 pages, seven components), and retained document/graph hashes match the accepted current-v3 output.

The zero-limit node PSI guard fired before terminal qualification. Around its trigger, the colocated MinIO container (exact Pod UID and stable cgroup) was near its 512 MiB limit; its `memory.events max` rose 419→446 and cgroup-full PSI rose 61.1 ms while node-full PSI rose 98.4 ms. OOM and OOM-kill stayed zero. This is direct concurrent memory-pressure evidence, but the precise object-store operation and sole cause remain unknown. The supervisor interrupted the inner phase; terminal manifest is `INCOMPLETE`. A later 28.676-second observer sample interval during cleanup also fails full-window coverage. Process recovery is **not accepted** and #51 must remain open.

Owned runtime was removed with UID preconditions; the evidence PVC and object prefix are retained. All 32 held Deployments remain exact/off. No automatic retry. See `pod-topology-v50/first-window-evidence/RESULTS.md` and the updated matrix. Next: review object-store operating bounds under the existing guard before a new-identity run; Pod drain/recovery and #44 handoff remain outstanding.
