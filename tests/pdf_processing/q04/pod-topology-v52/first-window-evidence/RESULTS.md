# BA: resource trial improved pressure; worker readiness race blocked acceptance

BA ran once with the frozen AH producer, current-v3 oracle, unchanged Q04 guards and the existing MinIO 768 MiB limit trial. All 11 pre-inference gates passed. The owned worker process changed generation 1→2 in one Pod. Temporal completed with a successful business result: 51 registered pages and seven components. The retained document SHA-256 was the accepted `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`; `checks.json` recorded the accepted current-v3 graph SHA-256 `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`. `drain-proof.json` retained the first group and recorded pages 6–10 on Activity attempt 2.

The unchanged outer node PSI guard did not fire. Across 1,391 complete 250 ms host samples (maximum interval 0.273 seconds), the exact MinIO container cgroup had `memory.max=805306368`, peak `memory.current=642871296`, zero new `memory.events max`, zero new cgroup-full PSI and zero OOM. Node-full PSI accumulated 35,275 microseconds, with no sample at or above 10 ms. This supports the hypothesis that the former 512 MiB object-service limit contributed materially to AY's pressure, but does not prove a sole cause or identify a specific object request. The 768 MiB limit remains a **trial setting**, not yet an accepted operating bound.

The inner phase then failed `incomplete resource coverage`: replacement worker `ready.json` was timestamped 1790324060.7507482, but its first telemetry sample was timestamped 1790324060.750903, 155 microseconds later. The existing coverage contract requires the first sample to precede readiness. The worker published Ready before its sampler task had necessarily run; this is a harness ordering defect. The initial worker showed the same ordering by 173 microseconds. Failure cleanup subsequently coincided with one incomplete process-attribution sample during parser exit; whether normal controlled shutdown would also produce such a sample remains unproven. `accepted.json` was not written and the durable terminal manifest is `INCOMPLETE`, so **process recovery is not accepted**.

All 54 retained inventory entries match their sizes and SHA-256 hashes. Supervisor, transport and read-only host observer stopped; UID-fenced owned runtime cleanup completed. BA PVC remains Bound at UID `6aade914-022f-44a1-bafc-e1e060fd5096`; raw evidence and prefix `q04/process-drain-pod-cgroup-20260925-ba/` remain retained. The 32 held Deployments and exact MinIO Pod were rechecked. No automatic retry occurred.

The next fresh-identity runner must use a worker that publishes Ready only after its first telemetry sample is committed. Keep all resource guards, deadlines, producer and oracle unchanged. Evaluate the trial operating bound only after an accepted terminal run.
