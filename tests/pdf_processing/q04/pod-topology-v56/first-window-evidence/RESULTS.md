# BE: native worker-process drain/recovery qualified

BE ran once with a fresh identity, frozen AH producer/bundle, BC worker readiness, BD complete-inventory mirror, and BE lifecycle-locked Pod-local signal helper. Local tests (19) and two-axis code review passed before execution. The existing kind/namespace, 32 exact/off held Deployments and exact 768 MiB MinIO Pod were verified before and after. All 11 pre-inference gates passed. No guard or deadline was relaxed and no automatic retry occurred.

Worker generation 1 was interrupted after five registered native pages; generation 2 recovered pages 6–10 on Activity attempt 2, retaining the first checked group. Both worker generations published their first resource sample before Ready (0.313 and 0.374 ms). Temporal history ended `WORKFLOW_EXECUTION_COMPLETED`, and its business result was complete with 51/51 registered pages, seven components and `processing_complete=true`. The document SHA-256 was the accepted `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`; the exact current-v3 graph SHA-256 was `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`.

The inner phase wrote `PASS native owned worker-process drain/recovery`. All 834 process/cgroup attribution samples were complete; maximum sample gap was 0.558 seconds, with no process, cgroup, OOM, PSI or memory violation. The final sample carried both cleanup markers. The controller copied the entire 50-file terminal inventory, and independent local size and SHA-256 verification found no mismatch. The durable manifest is `PASS_CANDIDATE`; this qualifies the **process drain/recovery** row for this exact runtime and producer.

The outer node PSI guard did not fire. The read-only host observer captured 1,388 samples with maximum start gap 0.274 seconds and exited cleanly; node-full PSI rose 10,276 microseconds, with maximum interval 2,096 microseconds. The exact MinIO cgroup under the temporary 768 MiB trial peaked at 805,036,032 of 805,306,368 bytes and logged eight new `memory.events max` events plus 16,095 microseconds of full PSI, without OOM or OOM kill. This is not a permanent operating-bound proof.

The workload exited 0, and owned children/scratch were absent. Controller export, UID-preconditioned Deployment/ConfigMap deletion, BE Pod absence, persistent-channel closure, retained Bound PVC UID `9d559446-d8ba-44b5-b9af-b019b10f15a1`, exact trial MinIO Pod identity and all 32 held Deployments were checked. Local raw evidence and object prefix `q04/process-drain-pod-cgroup-20260925-be/` remain retained. After BE, the temporary MinIO 768 MiB limit was restored to the original 512 MiB using Deployment UID and old-limit preconditions. The replacement Pod `objects-8865c4db6-b5tpl` reached Ready with zero restarts; Temporal was idle, object readiness returned HTTP 200, and the 32 held Deployments remained exact/off. Actual in-flight **Pod loss/recovery** and the supported-bounds report to #44 remain unqualified; #51 is not ready to close.
