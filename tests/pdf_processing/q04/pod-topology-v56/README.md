# Q04 BE: serialize Pod-local drain signal with attribution sampling

BD's business workflow and exact native output passed, but process attribution failed on one short-lived owned child (PID 171) observed during the deliberate drain signal. The Pod-local `Host.signal()` helper is the likely child. BE uses the existing `PDF_PROCESS_LIFECYCLE_LOCK` for the full lifetime of the normal signal helper, including subprocess creation and exit. The same lock already protects collector snapshots and production parser transitions; acquiring it before spawning prevents the helper from disappearing midway through a strict snapshot. If lock acquisition times out, the exact-owned emergency `force_stop()` runs without waiting for the lock and qualification fails. Local regression tests cover the held-lock subprocess lifetime and timeout cleanup. The lock retains its one-second acquisition deadline, and the sampler's gap and completeness checks remain unchanged.

BE also retains BD's all-inventory terminal mirror condition. It uses a fresh run identity and prefix, the frozen AH producer/bundle, the BC worker readiness fix, reviewed current-v3 oracle, unchanged resource guards and deadlines, the same temporary 768 MiB MinIO setting and exact read-only object Pod observer. One controlled execution is allowed, with no automatic retry. A pass would qualify process recovery only; Pod drain/recovery and the supported-bounds handoff remain open.
