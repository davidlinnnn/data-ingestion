# AT active telemetry-loss window

The single AT run (`q04-telemetry-loss-pod-cgroup-20260925-at`) reached the intended injection. At `1790274914.466346`, the native workflow had 5/51 registered pages and was parsing the next group; the worker's last sample was at `1790274914.199701`. The controller stopped the worker sampler, then raised `resource telemetry lost`. The owned cancel, result-task settlement, worker stop, publication audit, and Temporal history capture all succeeded. The worker reported no parser or scratch remaining. The publication audit found no complete registration.

The Pod workload nevertheless exited nonzero because the AT verifier rejected every Temporal `WORKFLOW_EXECUTION_COMPLETED` event. The retained terminal event is a JSON/plain business result with `status=failed`, `error.code=activity_budget_exhausted`, `processing_complete=false`, `canonical_accepted=false`, and 5/51 registered pages. Temporal completion was therefore **not** ingestion success. This is a verifier defect, not evidence that the telemetry guard missed the loss.

Controller cleanup retained the AT evidence PVC (UID `69eb04f6-b7c9-4026-b81d-04ddff964400`) and removed its Pod, Deployment, and two ConfigMaps using owned identity checks. The original run evidence is under `/private/tmp/q04-telemetry-loss-pod-cgroup-20260925-at/`; its failure export and `outer-cleanup.json` are the sources for this diagnosis. AT is not promoted to an accepted terminal gate because the workload did not seal a successful terminal evidence set. AU uses a new identity and a corrected business-result verifier; it does not rerun AT.
