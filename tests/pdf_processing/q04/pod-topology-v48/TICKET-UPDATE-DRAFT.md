# Draft for GitHub #51 — do not publish as a passing result

The new AW process-drain window used a fresh run identity and the frozen AH producer with unchanged resource guards. The harness stopped worker generation 1 during an active native group and started generation 2 in the same Pod. Temporal completed with 51 registered pages and one second Activity attempt for group pages 6–10. The consumer then used the historical method-off graph as its oracle for a current-v3 document and stopped. Offline verification shows AW's document bytes and graph exactly match the already accepted AN current-v3 native output; the existing reviewed current-v3 source oracle also passes. This was a harness oracle-selection defect. AW stopped without retry and still does not qualify the process-recovery acceptance row because terminal qualification and process attribution were incomplete.

The raw failure export, prefix and Bound evidence PVC are retained. Owned runtime cleanup passed and the 32 held Deployments remain off. Next: use the established reviewed current-v3 oracle in a new one-shot process window. Actual Pod drain/recovery and the operating-bounds handoff to #44 remain unproven. #51 is not ready to integrate or close.
