# AW process-drain result: harness oracle mismatch

The one controlled AW run (`q04-process-drain-pod-cgroup-20260925-aw`) passed pre-inference and reached five registered native pages while the next group was active. The existing Pod-local process-drain path stopped worker generation 1 and its parser, then started generation 2 in the same acceptance Pod. `drain.json` records `scope=owned_worker_process`, generations 1→2; both host snapshots have `pod=null` and distinct worker PIDs (112→174). Generation 1's terminal record confirms parser and scratch absence. This is **process drain**, not Pod replacement.

The workflow reached a business-complete result with 51 registered pages and seven OCR components. Its 137-event Temporal history ended `WORKFLOW_EXECUTION_COMPLETED` and contains one Activity start with attempt 2 (21 starts at attempt 1). The second start points to scheduled event 17, whose decoded operation is group pages 6–10. The consumer then rejected the full typed document because this harness used the historical `consumer.check_reference` directly. That compares the current-v3 document to the historical **method-off** graph `89bdb85383ed28ddcd3d5d6e6ea1e0669e96bc78c8b80ce31242a0d24060cc2d`. AW's actual graph `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972` and document bytes SHA-256 `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152` exactly match the already accepted current-v3 native results in AN. The existing `warm_v3_reference.reviewed_reference_checker` passed offline on AW's retained document and frozen AH bundle, preserving exact graph and source review. The earlier apparent 1,119→1,151 text-node difference is the documented method-off/current-v3 difference, not a newly demonstrated producer regression.

The nonzero workload produced a retained failure export. All 49 inventory file sizes and SHA-256 values matched the retained files, but the durable inventory is marked `INCOMPLETE`, as required for an unsuccessful qualification; independent process attribution is also incomplete. Thus the completed workflow cannot be promoted into a passed acceptance window. The outer controller recorded 819 host samples, no observed OOM, UID-fenced runtime deletion, a stopped host observer, and cleanup success. The evidence PVC remains Bound, UID `7c80264f-a94f-433a-b6bf-5c1acbf8e27f`; raw evidence and object prefix remain under `/private/tmp/q04-process-drain-pod-cgroup-20260925-aw/` and `q04/process-drain-pod-cgroup-20260925-aw/`. The 32 held Deployments were rechecked exact/off after cleanup. No automatic retry occurred.

Next, use the existing reviewed current-v3 oracle in a new process-drain harness, retain AW unchanged, and run once under a fresh identity. AW's incomplete attribution and terminal inventory still prevent retrospective promotion. Actual Pod drain/recovery and the #44 operating-bounds handoff remain open.
