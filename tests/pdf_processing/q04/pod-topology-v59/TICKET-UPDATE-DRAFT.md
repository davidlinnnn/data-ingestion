# #51 update draft — BH proves in-flight Activity Pod loss/recovery

BH ran once with fresh identity `q04-pod-loss-pod-cgroup-20260926-bh` at execution commit `87d0a58`. All 11 pre-inference gates passed. After five durable native pages, the controller deleted the exact in-flight Activity Pod, confirmed its runtime/scratch absent, and admitted one replacement Pod UID. Temporal started the interrupted Activity at attempt 2; only pages 6–10 retried, the earlier checked group stayed readable, and the business result completed with 51/51 registered pages, seven components, `processing_complete=true` and exact frozen document/full-graph output. The scoped finite Temporal execution budget was 300 seconds (actual about 199 seconds); outer/resource/output/cleanup gates were unchanged. Independent validation passed 521 old-Pod plus 480 replacement-Pod process/cgroup samples, 64 terminal inventory members and all 303 retained object entries.

Node full-PSI avg10 and VM OOM delta stayed zero. During this single bounded window, the exact MinIO 1 GiB Pod peaked at 536,842,240 bytes with no max/full-PSI events; the temporary setting was restored to 512 MiB. UID-fenced run-owned cleanup completed without errors; Temporal is idle, object health 200, all 32 held Deployments off, and the BH PVC/prefix remain retained. These measurements are not a permanent object-service sizing guarantee or evidence for restored historical Deployments/concurrency.

The Pod drain/recovery row is now proven for this exact producer/runtime. #51 has 15/16 cross-cutting gates proven and should remain open until the measured-bounds handoff to #44 receives mainline review/publication. The handoff draft is `SUPPORTED-BOUNDS-HANDOFF-DRAFT.md`; full BH evidence is `pod-topology-v59/RESULTS.md`. Please publish this update together with the reviewed #44 scope; do not close #51 solely from the BH pass.
