# BH — native in-flight Activity Pod-loss/recovery qualified

Execution commit `87d0a58`, identity `q04-pod-loss-pod-cgroup-20260926-bh`, ran once on the existing kind/namespace with no automatic retry. All 11 pre-inference gates passed. After five durably registered native pages, the controller deleted only Activity Pod UID `54a71233-7631-4e7c-9a56-402eed1dd807`, confirmed its runtime and scratch absent, and admitted replacement UID `62f25c96-5fda-4271-a3d9-e6e45228e161`. Temporal started the interrupted Activity at attempt 2 on that replacement. The checked first group stayed readable; only pages 6–10 were retried.

Temporal history began `2026-09-25T16:14:53.165Z` with a BH-scoped 300-second execution timeout and ended `WORKFLOW_EXECUTION_COMPLETED` at `16:18:12.104Z` (about 199 seconds). The **business** result is complete: 51/51 registered pages, seven selected and registered components, `processing_complete=true`. The accepted document and full graph SHA-256 equal the frozen current-v3 baselines (`70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152` and `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`). The 825-second outer work deadline, 300-second cleanup reserve, 4 GiB worker-Pod gate, zero OOM/full-PSI gates and all output requirements were unchanged.

Independent archive verification passed: 521 old-Pod and 480 replacement-Pod process/cgroup samples, zero uncommitted old-sampler tail bytes, replacement resource gate `PASS` with both final cleanup labels and maximum 0.464-second sample gap. The 64-member durable terminal inventory is `PASS_CANDIDATE` with `cleanup_complete=true`; the 75-member archive validated. Every one of 303 retained prefix objects (177,938,447 bytes total) passed independent readback. The node controller retained 1,286 samples, max full-PSI avg10 `0.0`, minimum MemAvailable `5,100,683,264` bytes and VM OOM delta zero.

The temporary exact MinIO 1 GiB Pod had 1,336 cgroup samples, peak charge `536,842,240` bytes, zero `memory.events max` and full-PSI deltas. This is one qualifying bounded workload, **not** a permanent MinIO sizing guarantee; its peak was only 28,672 bytes below 512 MiB, and historical 768 MiB runs had max/full-PSI events. MinIO was restored to 512 MiB with a new Ready Pod. Temporal is idle, object health returned 200, all 32 held Deployments remain off, and outer cleanup has no errors. Run-owned Deployments/Pods and ConfigMaps were removed with UID fencing. The BH evidence PVC `q04-pod-cgroup-bh-evidence-20260926-bh` (UID `7e30b14f-45e8-42fb-8072-543decca67d7`) and object prefix remain retained.

Disposition: **PASS_NATIVE_ACTIVITY_POD_LOSS_RECOVERY** for this exact producer/runtime and 300-second finite workflow budget. The Pod drain/recovery row is proven. The supported-bounds handoff to #44 still needs mainline review/publication and must state the exact scope and exclusions; #51 remains open until that final gate is done. The BH runner is locked against reuse.

Raw host evidence and archive: `/private/tmp/q04-pod-loss-pod-cgroup-20260926-bh/`.
