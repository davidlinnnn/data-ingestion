# AM first window: incomplete

Run identity `q04-method-pod-cgroup-20260924-am` executed once and stopped without retry. Fresh native completed: 51 registered pages, 7 OCR components, and the frozen document digest `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152`. The next, method-off request failed in its first group Activity with `worker_method_mismatch`; the business result has zero registered pages and no steps. Original-route replay did not run. Real method invalidation remains **unproven**.

The AM driver kept one warm parser across fresh and method-off. `src/pdf_processing/parse.py` binds the parser's current method at startup, and `src/pdf_processing/supervision.py` rejects a request whose expected method differs. The Temporal failure history and worker log both identify that mismatch in the first invalidation Activity. The worker stop and parser/scratch cleanup proofs succeeded. This is an AM orchestration defect, not evidence that the production invalidation contract fails. The method-off graph and downstream identities were never reached, so their behavior is unknown.

The terminal manifest is `INCOMPLETE`, with `cleanup_complete=true` and `workload_succeeded=false`. Its inventory includes the failed result, history, worker log, samples, and stop proof. Outer cleanup removed only AM-owned Deployment/ConfigMaps with UID preconditions, proved the old runtime and emptyDir absent, and retained the Bound AM evidence PVC. The 675 strict attribution samples were complete (maximum gap 0.575 s); no telemetry-loss or memory stop is indicated by the failure record. Source evidence remains at `/private/tmp/q04-method-pod-cgroup-20260924-am/failure-evidence`; its SHA-256 inventory is in `durable-terminal-manifest.json`.

Next window AN uses a new run identity, object prefix, Pod and evidence PVC. It must stop the fresh worker/parser and start generation 2 before the method-off trial, with both transitions covered by strict attribution. AM is never rerun or rewritten.
