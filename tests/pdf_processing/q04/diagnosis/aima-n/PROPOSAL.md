# Inactive proposal after N

N remains failed. No historical reference, producer, threshold or runtime runner
is modified by this proposal. User approval is required before adopting a
different acceptance rule, per the session's explicit execution restriction.

## AIMA image supplement

Keep the Q01 reference SHA-256
`7fb90a662e77789c0827a02e17d364718ea76f79e96718ff750a2883e227e92c` unchanged.
For fixture 08 only, allow precisely the 12 page and 9 picture `image` additions
after checking all metadata and decoded pixels against the fixed PDF/page crop.
Require exact equality of every other graph field. Preserve the complete actual
graph, including images, for fresh/restored/replay equality and digesting.
Continue the four-algorithm, eight-continuation-edge, OCR, relationship and
source-binding checks unchanged. Do not reuse the fixture-07 paragraph rule.

`analyze_graph_delta.py` implements the bounded offline comparison. Its real-N
replay test proves the original consumer still rejects; seven negative mutations
(text, caption, geometry, page pixel, picture pixel, DPI and missing image) fail.
`image-audit.json` binds all 21 verified image payloads. The independent render
uses the installed backend's 1.5x PDFium rendering and native-size resize; this
proves fidelity to that rendering contract, not a general PDF quality claim.

## Measurement: separate proposal, not enabled

N's two unknown process reads are exact confirmed exits under the already-existing
`classify_confirmed_exit_transition`: one fresh owned child and the worker.
Enabling that rule outside the approved fixture-07 window would broaden the
measurement contract, so it requires an explicit decision. The proposed boundary
is unchanged: adjacent complete enumerations and cgroup readings, same cgroup,
exact PID/start time, one confirmed exit, no PID reuse/read denial, bounded gap,
and never the resource peak. Keep raw PSS unknown and process attribution false;
only cgroup continuity may qualify. Never claim all process samples complete.

Also replace the ordering assertion on the asynchronous workflow query with
actual process-lifecycle evidence: the warm identity is gone before the fresh
assembly child appears, and no overlap occurs. Retain the query observation as
diagnostic evidence. Implement and test a new collector/runner version only
after the measurement disposition; do not reinterpret N as passed.

## Next execution and remaining scope

After explicit rule adoption, integrate the accepted candidates into a new
identity/prefix, validate full CLI/projected source/runtime contract and cleanup,
then independently review before one controlled execution. Keep the existing
kind cluster, all 32 held Deployments off, original parser budget 20, 4 GiB sample
gate/5 GiB hard limit, zero PSI/OOM, memory floors and deadlines. Failure stops;
no automatic retry. No capacity increase or new environment is proposed.

This still cannot close #51. Native/Wiki, lifecycle requalification of ACL/Keynote,
method invalidation, old-profile rejection, telemetry/process/Pod recovery and
the original five-document request20 warm sequence remain open. In particular,
current assembly handoff terminates the warm parser per document. That production
behavior must be repaired and requalified to meet the unchanged warm requirement;
this proposal does not relax it.
