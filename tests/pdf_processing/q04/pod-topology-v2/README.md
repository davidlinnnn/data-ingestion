# Q04 YOLO Pod/PVC window C

This directory contains the inactive, single-use fixture-07 fresh/restored/exact
replay topology prepared after readiness preflight B. Import and offline checks
do not contact Kubernetes. Runtime requires a new explicit authorization for the
exact scope digest in `RUNNER-MANIFEST.json`.

The earlier empty PVC `q04-pod-cgroup-a-evidence-20260919-a` is outside this
runner's ownership and cleanup scope. This window owns and retains only
`q04-pod-cgroup-c-evidence-20260919-c`.
