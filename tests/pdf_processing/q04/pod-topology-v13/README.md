# N — original AIMA fixture08 contract on the repaired Pod harness

One new original-pages99–110 fresh/restored/exact-replay window. Reuse the
original q04_runtime.profiles and consumer oracle: four complete algorithms,
selected regions with unresolved=reject, original representation policy and
exact continuation/source checks. No YOLO equivalence checker is installed.
Parser max_requests stays20. H–M evidence and all original source bytes remain.

Retain M's inherited workload-tree THP disable and persistent observers/startup
probe. All memory/PSI/OOM/coverage/transport/deadline guards remain unchanged.
The fresh index is pending until the full matrix, lifecycle attribution and
all-sample resource gate pass. No retry. Passing08 does not prove warm or drain.
