# O — oracle dependency missing from projected source

Execution `9d27751`, identity `q04-aima-pod-cgroup-20260920-o`. All 11 gates
passed; fresh completed 12 pages and all nine required OCR components. The
adopted 21-image supplement passed against fixed PDF pixels with exact equality
of every other graph field. The unchanged four-algorithm consumer then failed
loading `tests/pdf_processing/t09a/evidence/code-source-oracle.json` from the
projected workspace. Inspection also found the subsequent representation check
requires `tests/pdf_processing/q03/reviewed-representation.json`.

Both data files exist and pass in the full local checkout, but were omitted from
the ConfigMap projection. Earlier import/argument tests did not execute this
oracle path. The new projection regression reaches the exact consumer with
checked durable O artifacts: O reproduces FileNotFoundError; projecting both
unchanged files passes all 615 typed items, four algorithms and eight edges.
This offline replay diagnoses the harness; it does not promote O.

All 409 process samples are complete. The source-bound handoff and cancellation
measurement contract passes with no exit exception used. Maximum outer cgroup
memory is 1,745,375,232 bytes, minimum node available 6,315,626,496 bytes; node
full PSI and cgroup pressure totals/OOM increments are zero. There is no proven
capacity failure or Activity timeout. The consumer exception initiated cleanup.
Restored and exact replay did not run; no fresh index or overall acceptance exists.

41 sealed entries and 42 archive files match paths, sizes, SHA-256 and bytes,
including the inventory digest. Both channels closed, owned objects removed,
32 held Deployments unchanged at zero, historical PVC UIDs unchanged and Temporal
idle. O's evidence PVC/prefix is retained. See `outer-cleanup.json` and independent
verification. Raw graph/archive remain outside Git under the bound local output
and PVC; RAW-EVIDENCE-MANIFEST.json records all omitted private files.

Disposition: **FAIL_ORACLE_DEPENDENCY_NOT_PROJECTED**, no automatic retry.
P is a distinct corrected projection with a new identity/prefix and fresh review;
no acceptance standard, producer or guard changes beyond already approved O.
