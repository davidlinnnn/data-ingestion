# O — adopted AIMA image and measurement contract

User confirmed the concrete N proposal on 2026-09-20. Only fixture08 runs:
fresh/restored/exact replay with original max_requests=20. All 21 added images
must match fixed source pixels, every other graph field remains exact, and the
full actual graphs remain equal across modes. All four algorithm/eight edge,
OCR/source/relationship requirements stay unchanged.

Confirmed exit transitions preserve only cgroup continuity, never unknown PSS.
Handoff uses exact process identities, successful worker reap records and the
frozen reap-before-spawn barrier; query timing remains diagnostic. Sub-sample
order is not claimed as measured. No production or historical runner is changed.
