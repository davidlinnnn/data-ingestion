# AS first window: required relationship interruption and recovery passed

One controlled run of `q04-relationship-pod-cgroup-20260925-as` used source commit `3a3c163`, the unchanged AH private bundle and producer, prefix `q04/relationship-pod-cgroup-20260925-as/`, the original zero node/Pod full-PSI and OOM guards, and no automatic retry. All 11 pre-inference gates passed. Fresh native completed 51/51 registered pages and 7/7 OCR components. The required evidence child was interrupted at `first_page_decoded`, after a retained partial file manifest; its planned Activity failed once with `parser/execution_failed`, the workflow business result remained failed with `processing_complete=false`, and failed-plan publication audit passed. A new worker generation then completed recovery and exact replay, each at 51/51 pages and 7/7 OCR components. All three successful documents had frozen SHA-256 `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152` and full reference graph SHA-256 `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`. Temporal histories independently contain one failed Activity in the intentionally interrupted run and none in the three successful runs.

The unchanged all-sample gate passed 895/895 process/cgroup samples, with no PSI, OOM, memory or attribution violation. The outer controller retained 908 samples, maximum gap 0.443 seconds, minimum available memory 4,764,925,952 bytes and maximum Pod cgroup use 2,421,899,264 bytes. The separately owned host observer retained 1,478 continuous samples with maximum gap 0.275 seconds and was independently absent after stop. The 95-file terminal inventory passed independent exact-set, byte-size and SHA-256 readback; workload exit was zero. Both workers, parsers and scratch were absent. Owned Pod, Deployment and ConfigMaps were removed with UID preconditions; PVC `q04-pod-cgroup-as-evidence-20260925-as` remains Bound with UID `18709bdc-77e0-431b-b98d-6bb5e621e62f`. All 32 existing held Deployments remain exact/off. See [verification](INDEPENDENT-VERIFICATION.json).

The planned OCR phase trace is absent from the sealed inventory. `pod_workload_p.py` resets `PYTHONPATH` for its descendants and removed the AS-only startup-hook directory, so the hook did not load. This is a diagnostic harness defect; the workload's frozen producer and bundle hashes remained equal and the required relationship acceptance gate is independently proven. AS does **not** identify AQ's specific OCR process phase or allocator cause. A future trace design must bind the descendant `PYTHONPATH` before runtime; do not rerun AS or reinterpret absence of an AS PSI stop as proof that AQ's guard was false.

The remaining #51 gates are active telemetry-loss abort, actual process drain/recovery, actual Pod drain/recovery, and the supported operating-bounds handoff to #44. The ticket is not ready to close. Raw evidence remains under `/private/tmp/q04-relationship-pod-cgroup-20260925-as*` and on the retained PVC. No result has been posted to #51 from this session.
