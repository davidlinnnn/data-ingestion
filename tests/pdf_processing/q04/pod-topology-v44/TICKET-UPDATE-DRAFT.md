Q04 / #51 update (draft; not posted)

AS completed the current required-relationship interruption/recovery/replay gate under the existing kind cluster, 32 held Deployments, unchanged PSI/OOM/memory/deadline guards, and no automatic retry. Fresh, recovery and exact replay each completed 51 native pages and 7 OCR components and matched the frozen document and full-graph digests. The planned evidence child was killed after its first page was decoded; the interrupted workflow had one expected failed Activity and an incomplete business result, while the failed-plan publication audit passed. A replacement worker completed recovery and replay. All 895 process/cgroup resource samples, 1,478 host observer samples, 95 sealed evidence files and UID-fenced cleanup passed independent checks; the evidence PVC is retained Bound.

AR, the preceding new-identity attempt, stopped before inference on `bundle producer drift`; no workflow or object write started, and its owned runtime was cleaned with PVC retained. AS kept the frozen bundle and producer. AQ's earlier node/Pod PSI stop remains a real, locally attributed pressure event, but the exact OCR process/allocator cause is still unknown. The AS-only OCR phase hook did not load because the workload reset descendant `PYTHONPATH`; no phase trace was sealed. This diagnostic limitation does not affect the independent required-relationship pass.

The acceptance matrix now promotes only the required-relationship row. Active telemetry-loss abort, actual process drain/recovery, actual Pod drain/recovery and the #44 supported-bounds report remain unproven. #51 should remain open; no integration or closure claim yet.
