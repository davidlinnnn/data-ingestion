# BD: complete forensic mirror, one transient process sample blocks qualification

BD ran once with the frozen AH producer/bundle, BC worker readiness, the reviewed current-v3 native oracle, unchanged Q04 guards and the temporary 768 MiB MinIO limit. All 11 pre-inference gates passed. The owned worker-process drain replaced generation 1 with 2 and retried pages 6–10 on Activity attempt 2. Temporal history ended `WORKFLOW_EXECUTION_COMPLETED`; its business result was `complete`, `processing_complete=true`, 51/51 registered pages and seven components. The exact document and graph SHA-256 digests were `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152` and `9f1b0ef9ed561df0c4a5748d5c49554e8f5ad3bb38764d94a51d3433f4d94972`.

The inner process-attribution check failed before `phase-complete.json`: one of 836 samples (index 284) was incomplete. PID 171, an owned child of the trial coordinator PID 102, appeared and exited between process enumeration and `/proc` reading; PSS could not be read. The collector's resample remained unaccepted because the prior process identity was incomplete. The sample coincided with the deliberate drain signal and the parser's stopped state, making the Pod-local `Host.signal()` helper the likely short-lived child, but its command line was not captured and that exact identity remains unproven. The strict gate correctly failed; process recovery is **not accepted**. No automatic retry occurred.

The new BD transport condition was not exercised on a successful terminal because the workload failed first. The forensic export independently matches all 46 durable-inventory file sizes and SHA-256 hashes; the terminal manifest is correctly `INCOMPLETE`. The unchanged outer node PSI guard did not stop the run. The read-only host observer captured 1,396 samples (maximum gap 0.270 seconds) and exited cleanly. Exact MinIO cgroup memory peaked at 805,036,032 of 805,306,368 bytes, recorded 14 new `memory.events max` events but no OOM or OOM kill; node-full PSI increased 19,955 microseconds. This does not establish a permanent MinIO bound.

Owned supervisor, transport, Pod, Deployment and ConfigMaps were cleaned with UID preconditions. BD PVC `q04-pod-cgroup-bd-evidence-20260925-bd` remains Bound at UID `b2f2b51c-d4a1-4d92-955b-c9f3577adeec`; raw local evidence and object prefix `q04/process-drain-pod-cgroup-20260925-bd/` remain retained. Next: serialize the Pod-local drain-signal helper's entire subprocess lifetime with the existing attribution lifecycle lock, test the lock against a concurrent sample, review, then use a fresh identity for one controlled run. Do not relax the attribution gate or reuse BD.
