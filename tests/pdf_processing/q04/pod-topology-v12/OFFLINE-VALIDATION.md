# M local validation

THP policy tests first failed on the missing implementation. Tests cover
setting/readback, failure before launch, and real entrypoint ordering. The Linux
diagnostic separately checks actual inheritance across exec and AnonHugePages.
Full runner/transport/preflight/interruption/evidence tests and frozen projected
source/complete command validation are required before the one M window.
