# Post-BH warm-child correction: stage impact

PR #59 merged BH's qualified producer and then the `b98f510` warm-child correction. BH, AI and AJ retain their original immutable producer/runtime identities; their runtime artifacts are not relabelled as executions of `b98f510`.

| Change | Fingerprint and effect | Qualification decision |
| --- | --- | --- |
| `warm_child.py` SHA-256 `73164842da840051402f9e92fa5e6d900ab953d8fe3464c90a9b35540ce8b427` → `3e218a63ddffe8dfee8f5b1f7abf406ff88c387bb00ea91876b67a6f892c3e0e` | `compatibility.py` includes this file in the `group` dependency projection. Existing group identities must invalidate; assembly and later operations follow new group identities. No fingerprint bypass is allowed. | This correction updates only the active request used when classifying an `AssertionError` after a second warm-child request. It does not alter successful capture/restore conversion, output bytes, model/runtime policy, resource guards or Pod/worker lifecycle. Earlier successful fixture, warm and drain observations remain evidence for the bounded behavior they exercised, with their original producer identities. |
| Restore assertion after capture | Previously returned `parser/parser_execution_failed`; now `integrity/checkpoint_validation_failed` with the second request ID and child exit 1. | `test_warm_continuity` checks capture→restore and restore→capture mode, correlation, failure class and exit. `WarmParser` propagates the category/code as `ChildFailure`; `Processing.run` calls `reject`, whose integrity category is nonretryable. `Execution.produce` publishes only after its child completes, so this failure cannot publish a group/assembly result. |

The only affected acceptance behavior is failure classification on a second request. The successful fresh/restored/replay, cross-document warm reuse, resource and Pod-recovery paths are unchanged; their prior exact runs remain scoped observations, not new-runtime passes. The targeted regression and control-flow review requalify this correction without repeating the six-fixture/Pod-loss runtime. A new bounded runtime is required if a later change reaches successful conversion, fingerprint rules, retry/publication order, resource guards or lifecycle handling.
