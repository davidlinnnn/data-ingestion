# AO first window: node PSI stopped the relationship test

One controlled run of `q04-relationship-pod-cgroup-20260924-ao` stopped on the unchanged node memory PSI guard. The source commit was `37512b2`; the object prefix was `q04/relationship-pod-cgroup-20260924-ao/`. No automatic retry or threshold change occurred.

All 11 pre-inference gates passed. The fresh native request completed 51 pages and seven OCR components, matching the frozen document digest. The interrupted request had entered parsing but registered zero pages when the final controller sample measured node `full avg10=1.08` against the existing zero limit. The same sample showed 5,571,780,608 available node bytes (floor 1,610,612,736), Pod memory 1,844,002,816 bytes (limit 5,368,709,120), and no Pod or VM OOM kill. The three preceding 250 ms samples read zero PSI. The triggering sample is retained among 680 controller samples. These measurements establish a real guard crossing; they do not identify the process responsible for node pressure or prove spare capacity at the moment of the stall.

The controller recorded the stop at 15:40:02.365 UTC, signaled the owned supervisor at 15:40:02.459, and Temporal recorded workflow cancellation at 15:40:02.718. Temporal then marked the execution `COMPLETED` with business `status=failed`, `processing_complete=false`, zero registered pages, and `activity_budget_exhausted`. This is the workflow's fallback classification after interruption, not evidence that its planned Activity retry budget was exhausted. There was no Activity failure event. The test-only required-evidence child interruption had not been reached; neither recovery nor replay ran. The failed plan had no complete publication.

The 50-file terminal inventory passed independent file-size/hash readback. Its status is `INCOMPLETE`, with worker, parser, scratch, Pod and owned Kubernetes objects absent. The AO evidence PVC remains Bound, and all 32 existing held Deployments retain their UIDs and zero desired/ready replicas. Raw evidence and the original object prefix are retained under `/private/tmp/q04-relationship-pod-cgroup-20260924-ao/` and on the PVC. [Independent verification](INDEPENDENT-VERIFICATION.json).

Next, inspect the node-level pressure source and require a stable pre-admission window under the same guards. A later attempt must use a new run identity, prefix and PVC. The relationship interruption/recovery/replay, active telemetry-loss, process/Pod recovery, and #44 bounds handoff remain unaccepted. #51 remains open and is not ready for integration or closure.
