Q04 AO used a new identity/prefix for one controlled required-relationship interruption attempt. All 11 pre-inference gates passed and fresh native completed 51 pages/seven OCR components with the frozen document digest. During the next request's first parsing Activity, the unchanged node memory PSI guard stopped the run (`full avg10=1.08` vs 0). The triggering sample was retained; node available memory remained above its floor and Pod/VM OOM counts stayed zero. Temporal execution completed only with business failure (`activity_budget_exhausted`, `processing_complete=false`, zero registered pages) after supervisor cancellation. The required-evidence injection, recovery and replay were not reached.

Fail-stop cleanup and independent readback passed: 50 terminal files matched hashes, the failed plan had no complete publication, owned runtime/emptyDir were removed, AO PVC remains Bound, and all 32 held Deployments remain exact/off. The node pressure source is unknown. We will inspect it and require a stable baseline before any new-identity attempt under unchanged guards. #51 remains open; no integration or closure claim.
