Q04 AQ used one new-identity required-relationship attempt with unchanged guards and a host-cgroup PSI observer. All 11 pre-inference gates passed; fresh native completed 51 pages and seven OCR components with the frozen document digest. During interrupted native, the controller stopped on node/Pod `full avg10=0.18` versus the existing zero limit. The retained trigger showed no OOM and no memory-floor or Pod-limit breach. Continuous host PSI samples mapped the dominant stall increments to the AQ Pod UID and container cgroup, while process samples showed warm-parser/fresh-child overlap and concurrent global direct reclaim. The exact process/allocator cause remains unproven, so no threshold or capacity change is proposed.

Temporal cancellation followed the controller stop. Its execution `COMPLETED` only with failed business result (`activity_budget_exhausted`, `processing_complete=false`, 51 registered pages, two of seven components); no Activity failure event or required child interruption hook occurred. Recovery and replay did not run. Fail-stop cleanup and independent evidence checks passed: 1,269 continuous host samples, 50 terminal files with exact sizes/hashes, worker/parser/scratch and owned runtime absent, AQ PVC Bound, and 32 held Deployments exact/off. #51 remains open. Next: targeted review of allocator/reclaim and overlapping parser/child behavior, then a reviewed new-identity diagnostic or fix under the same acceptance standard.
