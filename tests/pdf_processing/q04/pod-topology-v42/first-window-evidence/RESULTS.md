# AQ first window: pressure reached the AQ Pod during interrupted native

One controlled run of `q04-relationship-pod-cgroup-20260925-aq` used source commit `75fa9b8`, prefix `q04/relationship-pod-cgroup-20260925-aq/`, unchanged memory/PSI/deadline guards, and a separately owned host-cgroup observer. It stopped once, with no automatic retry. All 11 pre-inference gates passed. Fresh native completed 51 pages and seven OCR components, with frozen document SHA-256 `70673bdc5cb548d37c81efa91bb1c5460f71f94148bdb5d1218a319c6af9f152` and no failed Activity events.

During the interrupted native request, the controller's retained trigger sample at 2026-09-24 17:18:27.311 UTC read node and AQ Pod memory `full avg10=0.18` against the unchanged zero limit. Available node memory was 5,135,486,976 bytes (floor 1,610,612,736); AQ Pod memory was 2,166,820,864 of 5,368,709,120 bytes; Pod/VM OOM kills were zero. The controller recorded the stop at 17:18:27.313 UTC. Host-cgroup samples at about 1.536 and 1.274 seconds before that stop measured node `full` increments of 14,643 and 22,677 microseconds. The AQ Pod UID `b826f5eb-4f51-48bb-a9bc-0fcb7c84e472` cgroup rose 14,596 and 21,698 microseconds in the same intervals; its container ID `1777c9e6...` leaf also rose. Over the ten seconds before to one second after the stop, node `full` rose 37,559 microseconds and the AQ Pod cgroup rose 36,294 microseconds. Cgroup PSI is hierarchical and independently sampled, so these counters must not be added or interpreted as exact shares of the node total. They establish that AQ's own Pod experienced pressure, unlike AP's incomplete private-namespace observation.

Pod-local process samples around the event show a warm parser near 1.59 GiB PSS and a fresh owned child growing from about 268 to 534 MiB PSS while the Pod memory charge rose. VM `allocstall_movable` and direct-reclaim counters rose in the same short interval. This is a correlation, not proof of which process or allocator caused the stall; THP causality and why direct reclaim occurred with apparent available memory remain unknown. No memory limit, OOM, memory floor, or deadline guard fired. The PSI guard should not be relaxed based on these measurements.

Temporal requested cancellation at 17:18:27.364 UTC, after the controller stop; the outer supervisor received its stop signal at 17:18:27.560 UTC. The execution then recorded `COMPLETED` only with business `status=failed`, `processing_complete=false`, `activity_budget_exhausted`, 51 registered pages and two of seven selected OCR components. There was no Activity failure event; the budget code is the workflow's cancellation fallback, not proof that its planned retry budget was exhausted. The required-evidence child interruption hook had not fired, so recovery and exact replay did not run. Inner `worker-stop` recorded `CancelledError` during abort, while independent outer cleanup proved worker, parser and scratch absent. The failed plan's publication audit completed; no interrupted acceptance file exists.

The host observer retained 1,269 continuous samples across 153 host-visible cgroups, largest interval 0.274 s, then exited after an ID-fenced stop; its container was independently absent. The 50-file `INCOMPLETE` terminal inventory passed independent exact-set, byte-size and SHA-256 readback. AQ Pod, Deployment and ConfigMaps were removed with UID preconditions; its evidence PVC remains Bound with UID `706f50ca-4218-4450-8a39-63236105e32d`. All 32 existing held Deployments retain exact UIDs and zero desired/ready replicas. Raw evidence remains under `/private/tmp/q04-relationship-pod-cgroup-20260925-aq*` and on the retained PVC. [Independent verification](INDEPENDENT-VERIFICATION.json).

AQ proves the node PSI stop involved its own Pod, but does not prove the workload's precise stall cause or the required-relationship acceptance row. Do not retry AQ or increase memory by assumption. The next useful work is a targeted review of the warm-parser/fresh-child overlap and allocator/reclaim evidence, followed by a separately reviewed, new-identity diagnostic or implementation change that preserves the same acceptance standard and guard. #51 remains open and is not ready for integration or closure.
