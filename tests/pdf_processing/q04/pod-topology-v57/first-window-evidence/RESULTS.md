# BF — node PSI stopped native before Pod-loss injection

Execution commit `7224960`, identity `q04-pod-loss-pod-cgroup-20260925-bf`, ran once on the existing kind/namespace. The user specifically authorized one temporary shared MinIO 1 GiB trial and 512 MiB restoration. Outer admission and all 11 Pod pre-inference gates passed. The native workflow started and reached parsing, but registered zero pages before the unchanged node memory PSI guard stopped the runner. No in-flight Pod deletion was injected, no restored/exact replay ran, and BF proves no new #51 acceptance row.

The saved rejecting controller row at `1790345148.0945063` has `psi_full_avg10=0.18`, MemAvailable `6,366,519,296` bytes and VM OOM count 0. Over the preceding controller sample, node `pgscan_direct` rose by 19,501 and `thp_fault_alloc` by 35. The Activity Pod's strict process/cgroup row 59 ms before that controller row also has full-PSI avg10 `0.18`, cgroup charge `1,121,710,080` bytes, zero OOM events and a warm-parser PSS of `1,079,944,192` bytes; its preceding row had no full-PSI avg10 and a smaller parser PSS. This ties the pressure to the Activity Pod during warm-parser growth, but does not prove which allocation or kernel path caused the reclaim burst. The exact 1 GiB MinIO Pod observer retained 284 samples, a peak of `159,690,752` bytes, zero `memory.events max` delta and zero full-PSI delta. No supported object-service memory bound follows from this short run.

Temporal history began at `1790345140.3547552`. The parsing Activity was scheduled at `1790345140.4937923` but had no `ACTIVITY_TASK_STARTED` event. The node PSI sample preceded `WORKFLOW_EXECUTION_CANCEL_REQUESTED` at `1790345148.8204472`; the workflow then emitted an Activity cancel request and `WORKFLOW_EXECUTION_COMPLETED` at `1790345148.831743`. Its **business** result is `failed/activity_budget_exhausted`, `processing_complete=false`, zero registered pages. The supervisor interruption was recorded at `1790345148.8174393`. The `activity_budget_exhausted` result followed cancellation; it is not proof of an earlier Activity failure or successful ingestion.

The first failure tar was captured while worker-owned `resource-attribution.jsonl.inflight` and `worker-1/samples.jsonl` were still changing, so `export_evidence()` correctly rejected its before/after fingerprint. A UID-fenced, read-only helper Pod recovered the retained PVC after runtime cleanup and was removed. The recovered copy has 35 files, including three files written after the first tar (`workload-exit.json`, `cleanup-complete.json`, `durable-terminal-manifest.json`). The terminal manifest itself is **INCOMPLETE** and fails readback because `resource-attribution.jsonl.inflight` grew by 3,877 bytes after sealing. It must not qualify terminal evidence. The raw PVC, first tar, host traces and read-only recovery copy remain available; the first tar SHA-256 is `771a4b9630d3ea5ad166b1127c5b411a211ac95e4748c68edf2428042ef4aa0a`, and the recovered 35-file inventory SHA-256 is `47da829cfd277d34660e7c7dfdb4a2e6859076c53b1b35c5d9e89f3f3c70bba0`.

The retained BF object prefix has 28 objects totaling 33,672,392 bytes. Every object was independently read back for size, ETag and SHA-256; `failure-object-readback.json` SHA-256 is `9a93942967bb5d7ad2d9c9fbec48abbaed56cbfe219a6450b2b27b8cd51f3913`. The BF worker/coordinator Deployments, Pods and ConfigMaps were removed with UID-fenced cleanup; the evidence PVC `q04-pod-cgroup-bf-evidence-20260925-bf` (UID `09c5d6b0-f34b-4943-aad3-31bc38d85a68`) and object prefix were retained. MinIO was restored to 512 MiB with a new Ready Pod; Temporal is idle, object readiness is 200, and all 32 held Deployments remain exact/off.

Disposition: **STOP_NODE_PSI_BEFORE_POD_LOSS; TERMINAL_EVIDENCE_INCOMPLETE**. The BF runner is locked again (`runtime_authorized=false`); do not reuse this identity. A local-only follow-up changes failure cleanup to stop the Activity Pod writer before exporting evidence through the coordinator. Its regression tests and two-axis review pass, but that fix has not been exercised live. The next window needs a fresh identity/prefix and a diagnosis of the node PSI burst; no guard or threshold was relaxed.

Raw host evidence: `/private/tmp/q04-pod-loss-pod-cgroup-20260925-bf/`. Read-only PVC copy: `/private/tmp/q04-bf-pvc-recovery/`. The original PVC and object prefix remain in the cluster.
