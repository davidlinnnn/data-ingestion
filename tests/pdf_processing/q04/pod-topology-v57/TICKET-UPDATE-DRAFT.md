# #51 update draft — BF stopped before Pod loss

BE proved worker-process drain/recovery under its exact producer/runtime: native 51/51 pages, seven components, pages 6–10 on Activity attempt 2, frozen document/graph hashes, all 834 process/cgroup samples, terminal inventory and cleanup. It did not delete an in-flight Kubernetes Pod, so the Pod drain/recovery gate remains unproven. The supported-bounds report also remains unproven/unpublished; #51 should stay open (14/16 cross-cutting gates proven).

BF ran once under a new split-Pod identity after local source/launch, interruption, object-cgroup and cleanup tests. Outer admission and all 11 pre-inference gates passed. During native parsing, the saved node sample reached `psi_full_avg10=0.18` with 6.37 GB MemAvailable and no VM OOM; an Activity Pod row 59 ms earlier also showed full-PSI avg10 0.18 while the warm parser grew, without OOM. The unchanged PSI guard stopped the run at zero registered pages, before the planned Pod-loss injection. Temporal cancellation followed the PSI sample; its execution is COMPLETED but its business result is failed/`activity_budget_exhausted`, `processing_complete=false`, zero pages. BF proves no Pod-loss row.

The specifically approved temporary MinIO limit was restored from 1 GiB to 512 MiB. Its exact cgroup had 284 samples, under 160 MiB peak, and zero max/full-PSI delta during this short run; this does not establish a permanent bound or explain node PSI. The first failure archive changed during export, and the retained PVC terminal manifest is incomplete because a worker-owned measurement file kept growing. A UID-fenced read-only recovery preserved the 35-file PVC copy. The run's 28 object-prefix entries (33.7 MB) passed independent size/ETag/SHA readback. Worker/coordinator Pods and ConfigMaps were removed, the BF PVC/prefix retained, MinIO is Ready at 512 MiB, Temporal idle and all 32 held Deployments off.

The BF runner is now locked against reuse. A local-only fix stops the Activity Pod writer before failure-evidence export; 36 BF tests and two-axis review pass, but it has not run live. Next: diagnose the node PSI burst without weakening guards, then plan one fresh-identity Pod-loss window and a measured-bounds handoff. #51 remains open at 14/16 proven gates; do not claim Pod-loss acceptance or publish a supported MinIO bound. See `pod-topology-v57/first-window-evidence/RESULTS.md`.
