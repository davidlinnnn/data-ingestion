# #51 update draft — BF preparation, no live acceptance claim

BE proved worker-process drain/recovery under its exact producer/runtime: native 51/51 pages, seven components, pages 6–10 on Activity attempt 2, frozen document/graph hashes, all 834 process/cgroup samples, terminal inventory and cleanup. It did not delete an in-flight Kubernetes Pod, so the Pod drain/recovery gate remains unproven. The supported-bounds report also remains unproven/unpublished; #51 should stay open (14/16 cross-cutting gates proven).

BF is a new-identity split-Pod runner with a separate coordinator and replaceable Activity Pod. Its offline source/launch contract, parser hold and Pod UID transition, triggering-sample persistence, object-cgroup observer, all-prefix object readback and owned cleanup have passed local BF regression tests (33 tests). The node-side object observer also completed a read-only idle smoke against the exact current MinIO cgroup: nine samples, zero new max/full-PSI events and verified remote-process stop. BF has **not run live**; its runner manifest remains `runtime_authorized=false`, and no BF PVC, Pod, workflow or object prefix has been created.

BE's temporary 768 MiB MinIO setting peaked at 805,036,032 / 805,306,368 bytes and recorded eight `memory.events max` events plus 16.095 ms full PSI, without OOM. The shared `objects` Deployment is now back at 512 MiB (UID `9e69720a-ff80-4aa5-9941-14925dfdf981`, one Ready Pod, zero restarts). An inactive, reviewed 1 GiB single-window trial with exact-UID patch and 512 MiB restoration is prepared, but is not connected to BF. It would restart this shared service and requires specific approval; 1 GiB would be a measured trial, not a supported bound. Keep unchanged PSI/OOM/memory/deadline and attribution gates, stop after one failed run, retain all evidence, and do not repeat H.

Next: after specific authorization and final offline orchestration review, recheck kind/namespace, the 32 held Deployments, service health and available memory; perform one BF window with a fresh identity/prefix; seal the complete business, resource, Pod replacement, object and cleanup evidence. Publish #51/#44 conclusions only from that evidence. No #51 acceptance or closure is claimed here.
