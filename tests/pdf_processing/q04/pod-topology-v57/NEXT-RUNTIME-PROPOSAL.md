# Next Q04 Pod-loss window: fresh BG identity

BF is terminal failure evidence and its runner is locked. The corrected Activity-supervisor THP policy and failure-export ordering are local-only; neither is qualified by a live run. The [BF diagnosis](first-window-evidence/THP-DIAGNOSIS.md) supports testing that correction, but does not prove THP caused the PSI stop.

Prepare a new run identity `q04-pod-loss-pod-cgroup-20260925-bg`, object prefix `q04/pod-loss-pod-cgroup-20260925-bg/`, evidence PVC `q04-pod-cgroup-bg-evidence-20260925-bg`, and fresh host output under `/private/tmp/q04-pod-loss-pod-cgroup-20260925-bg/`. Do not reuse any BF runtime object, prefix or PVC. Freeze the new projection and runner manifests at one execution commit; keep automatic retry off. Keep all node PSI, Pod PSI, OOM, memory-floor, process/cgroup coverage, source/hash, business-result and deadline gates unchanged.

Before runtime, check the actual kind/namespace, all 32 held Deployments, Temporal idle, object health and service/Pod UIDs. The shared MinIO Deployment is currently Ready at 512 MiB. BE's 768 MiB run recorded eight `memory.events max` events and 16.095 ms full PSI, so 512 or 768 MiB is not a supported setting for this workload. The earlier one-time BF 1 GiB trial was consumed and restored; another temporary 1 GiB trial requires a separate explicit decision. If approved, fence the existing `objects` Deployment UID/resource version, run one bounded window at 1 GiB, sample its exact Pod cgroup, and restore 512 MiB in terminal cleanup. This is a trial setting, not a proposed permanent bound or a change to acceptance criteria.

In the new window, verify `activity-memory-policy.json` for each Activity generation reports `thp_disabled=1` before worker spawn. Preserve the rejecting sample if any gate stops the run, stop writer Pods before evidence export, and verify the terminal inventory by independent readback. If zero-PSI and pre-inference gates pass, inject exactly one UID-fenced in-flight Activity Pod loss after five durably registered pages; require the existing 51-page, seven-component, attempt-2, hash, old-Pod absence and cleanup contracts. On any failure stop without retry and retain the PVC/prefix. Report measured object-service bounds only after a complete qualifying run; leave #51 open until Pod loss and supported bounds are proven.

The BG runner/projection and 37 BG tests are now built and pass locally. The user explicitly authorized the requested **one temporary BG 1 GiB MinIO trial with restoration to 512 MiB** in the immediately following conversation turn. This authorization does not cover automatic retry, a permanent shared-service change, relaxed acceptance gates or a second BG window. The runtime must still pass final source/manifest review and live preflight before the single attempt.

The user subsequently instructed execution of this exact next step. Automatic approval review accepted enabling the BG runner's one-window gate; the runner manifest is now `runtime_authorized=true`. No other identity is authorized by this change.
