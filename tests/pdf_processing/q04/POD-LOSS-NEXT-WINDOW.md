# Q04 remaining in-flight Pod-loss qualification

BE proves worker **process** drain/recovery. It does not prove Kubernetes Pod loss: the Q04 trial coordinator and Activity worker ran inside the same Pod. Deleting that Pod would kill the coordinator and prevent it from observing recovery. The R3 precedent instead used the existing separate `coordinator` Pod plus a replaceable Activity Pod; R3's old Pod disappeared 32.1 seconds after deletion and its exact native work then recovered. Those durations are observations, not new Q04 thresholds.

## Execution design

1. Keep the 32 held Deployments at their exact zero replica values. Use the existing kind, namespace, node, Temporal/object services and the existing `coordinator` Pod only after rechecking its UID/image/health and absence of running workflows. Project the frozen AH producer and current-v3 oracle by exact source hashes into a **new** run-specific coordinator path; do not alter the historical T09a ConfigMaps or directories. Create a separate run-owned Activity Deployment/Pod and PVC with a new identity/prefix.
2. The host controller owns Kubernetes injection and evidence. The coordinator Pod submits and queries the native Temporal workflow; the Activity Pod runs the same current-v3 worker/child path. Start independent 250 ms worker-Pod cgroup/process samplers before request admission and retain old/new Pod traces separately. Retain the existing node PSI, OOM, available-memory floor, per-Pod 4 GiB resource gate, telemetry-gap and finite deadlines. The intentional deleted-Pod interval must be an explicit, bounded replacement transition, never an ordinary missing sample treated as complete coverage.
3. Wait for five **durably registered** pages, record their readable artifacts and the in-flight group, then delete only the exact run-owned Activity Pod using UID preconditions. Verify old Pod/CRI container/scratch absence. Admit only one new Pod UID with zero restarts, start its sampler and worker, then require Temporal completion, 51/51 pages, seven components, pages 6–10 at attempt 2 and all prior checked groups retained. Compare the exact frozen document and full graph hashes; independently read every retained object and terminal-inventory member.
4. Fail closed on any mismatched UID, missing or stale sampler, PSI/OOM/memory guard, wrong Activity retry, incomplete business result, hash difference, missing old-runtime cleanup or deadline. One new identity, one run, no automatic retry. Preserve all failed evidence and PVC/object prefix. Remove only run-owned objects with UID preconditions, return Temporal to idle and check service health and the 32 held Deployments.

## Current operating-state decision

The temporary 768 MiB MinIO trial was restored to its original 512 MiB limit after BE. BE's exact MinIO cgroup still recorded eight `memory.events max` events and 16.095 ms of full PSI under 768 MiB, although the outer node PSI guard did not fire. A new Pod-loss runner must review object-service memory behavior and its temporary capacity setting before runtime; neither 512 MiB nor 768 MiB is a demonstrated supported bound. Do not weaken the node/Pod PSI or attribution gates to force a pass. Until the split coordinator and two-Pod telemetry contract are implemented, locally tested and reviewed, no in-flight Pod-loss claim is available for #51 or #44.
