# AU active telemetry-loss acceptance

One controlled AU execution (`q04-telemetry-loss-pod-cgroup-20260925-au`) passed the active telemetry-loss negative gate. The frozen AH producer/bundle, existing kind namespace, 32 held Deployments, and node/Pod PSI, OOM, memory-floor and deadline guards were unchanged. All pre-inference gates passed; no automatic retry ran.

At five registered native pages, the worker was parsing a distinct next group. Its sampler stopped at `1790275554.4046566` injection time. The controller then raised `resource telemetry lost`, requested owned cancellation, settled the result task, stopped the worker, audited publication and fetched Temporal history; each cleanup outcome passed. Temporal execution `COMPLETED` carries a **failed** business result: `activity_budget_exhausted`, 5/51 registered pages, `processing_complete=false`, `canonical_accepted=false`. There is no complete registration. The independent attribution collector required and observed the cancel callback in sequence and qualified the guard-failure measurement.

All 323 process/cgroup samples passed the unchanged resource gate with no attribution, memory, OOM or PSI violations. The controller recorded 313 samples (maximum gap 0.401 s, minimum available 5,463,732,224 bytes, maximum Pod cgroup 1,733,230,592 bytes); the read-only host observer recorded 841 continuous samples and stopped cleanly. The 42-file terminal inventory passed independent exact-set, length and SHA-256 readback in `verify_au.py` / `INDEPENDENT-VERIFICATION.json`. Pod-local terminal cleanup reports one worker generation, no worker, owned child or scratch remaining.

Controller cleanup removed the AU Pod, Deployment and two ConfigMaps with identity checks; the retained evidence PVC is Bound at UID `dfa79cf0-209a-4fe6-a8ac-aee0405a24a1`. A separate live check confirmed all 32 held Deployment UIDs and zero desired/ready replicas. The raw evidence is at `/private/tmp/q04-telemetry-loss-pod-cgroup-20260925-au/`. `outer-cleanup.json` has `terminal_stop_proven=false` because the controller did not initiate an emergency stop after natural success; workload exit zero, terminal markers and absent owned runtime prove cleanup.

This proves only active telemetry-loss fail-closed. Actual process drain/recovery, in-flight Pod drain/recovery and the supported operating-bounds report remain unproven. AQ's exact OCR process/allocator cause remains unknown.
