# BG — Pod loss occurred; workflow timed out before completion

Execution commit `73c82bb`, identity `q04-pod-loss-pod-cgroup-20260925-bg`, ran **once** on the existing kind/namespace. All 11 pre-inference gates passed. The first Activity Pod durably registered five pages; the controller then deleted that exact Pod UID and admitted one replacement UID. Temporal started the interrupted parsing Activity at attempt 2 on the replacement Pod. Both Activity supervisors recorded `thp_disabled=1` with child inheritance. The old Pod runtime and scratch were confirmed absent.

Temporal history fixes the primary stop: workflow start `2026-09-25T15:34:39.978Z`, configured `workflowExecutionTimeout=180s`, `WORKFLOW_EXECUTION_TIMED_OUT` at `15:37:39.980Z`. The last retained progress row showed 51 registered pages but only 3/7 registered components, `processing_complete=false`, status `enriching`. This is **not** a successful ingestion or a Pod-loss acceptance pass. The replacement took enough of the fixed 180-second workflow budget that the full work did not finish; the trace does not establish that a longer budget alone would guarantee success.

The controller's final `Activity worker did not stop` timeout was secondary and misleading. The replacement worker wrote `stopped.json` with parser/scratch absence at `1790350660.825327`; its 379-sample resource gate had no memory, OOM, PSI, attribution or gap violations. It failed solely because the final sample lacked `owned_cleanup_finished`, although it had `post_cleanup_sample`. The split-Pod supervisor had not emitted that required cleanup observation. The controller waited for gate `PASS` without surfacing the stored `FAIL_RESOURCE_GATE`, then timed out after 75 seconds. Local test-first corrections now emit the observation only after reading the worker's stopped proof and report a failed gate directly. Those corrections have **not** run live; BG is locked against reuse.

Node controller: 1,221 samples, max full PSI avg10 `0.0`, minimum MemAvailable `5,086,441,472` bytes, VM OOM delta zero. Exact MinIO Pod: 1,268 samples, peak charge `445,997,056` bytes, zero `memory.events max` and full-PSI deltas under the temporary 1 GiB setting. This one incomplete run does not establish a supported MinIO bound. MinIO was restored to 512 MiB with a Ready replacement Pod; Temporal idle and object health 200 were verified. Both run-owned Deployments/Pods and ConfigMaps were removed with UID fencing. The BG evidence PVC `q04-pod-cgroup-bg-evidence-20260925-bg` (UID `daf31908-691f-4326-9cb2-5aef4ba2778a`) and object prefix were retained. Outer cleanup reports no errors. The failure archive exists, but its terminal manifest records `cleanup_complete=false`; it is failure evidence, not a passing terminal inventory.

Disposition: **WORKFLOW_EXECUTION_TIMEOUT_AFTER_POD_LOSS; ACTIVITY_CLEANUP_OBSERVATION_MISSING; NO_ACCEPTANCE**. #51 remains 14/16 proven gates; Pod-loss recovery and supported bounds remain unproven. Do not rerun BG. Before another window, review the existing 180-second Temporal execution budget against the observed Pod replacement and remaining work; any change to an acceptance criterion requires a separate decision.

Raw host evidence: `/private/tmp/q04-pod-loss-pod-cgroup-20260925-bg/`. Retained archive: `failure-evidence.tar` there. Historical execution source is commit `73c82bb`.
