# AR first window: stopped before inference

AR used run ID `q04-relationship-pod-cgroup-20260925-ar`, prefix `q04/relationship-pod-cgroup-20260925-ar/`, and source commit `b26bfbc`. The node identity and 32 held Deployment UID/replica contracts passed. Admission, Pod readiness, source-hash, model, package, cgroup, Temporal, and object-store gates passed. The `configuration` gate failed with `bundle producer drift`: the AR-only projected `ocr.py` hash was bound by the source manifest, while the copied AH `inputs.json` still carried the original OCR producer hash. No workflow, inference, or object write started. This is an acceptance-tool fixture contract error, not evidence of OCR or memory behavior. The AR identity is consumed and will not be reused.

The controller fail-stopped with `FAIL_PRE_INFERENCE`. [Cleanup](/private/tmp/q04-relationship-pod-cgroup-20260925-ar/outer-cleanup.json) reports owned Pod, Deployment, and ConfigMaps removed with UID preconditions, old runtime absent, and the AR evidence PVC retained Bound (`bec1523d-44c0-4b40-9c0f-b01b8f4678b6`). The host observer recorded 506 continuous samples, max gap 0.271 seconds, then stopped with its container independently absent. Raw controller and observer evidence remains under `/private/tmp/q04-relationship-pod-cgroup-20260925-ar*`.

Next preparation must make the diagnostic producer hash explicit in a new derivative input bundle while retaining byte-identical PDFs, oracles, and test files. This does not change the acceptance gates. A new run identity is required. If the OCR phase trace is absent after a future runtime, classify the process-level cause as unknown rather than inferring a phase.
