# T09a predeclared bounded qualification

Baseline: 765d5483a61f8648f015e2f7f5538c54b62b6198. Declared before inference.
Seam: captured versioned source → real Temporal Activities and shared MinIO →
attributable v3 required_evidence_v1 complete result or explicit failure.

## Fixed workload and configuration

Full native 51-page arXiv 2303.18223v1; full WikiSkill 28 pages; full YOLO26 15
pages; AIMA physical pages 99–110 (printed 80–91), contiguous section 3.4
Uninformed Search Strategies with preceding queue/cost context. This is 12 pages,
not the complete chapter or 1,151-page book. It includes algorithms, figures,
table, footnotes, formulas and the previously uncertain physical-page-104
uniform-cost paragraph. Physical→printed offset verified from native headers.
ACL physical 2–4 and one-page Keynote are bounded supplemental regressions.
Original hashes must match the catalog/fixed fixture inventory before uploading.

Five pages/group, one Activity/parser slot, CPU four threads; native-v1 only.
No synthetic unsupported profile: the supported OCR render-scale policy (3 then 4)
provides a real profile transition, with bounded process replacement when changing
frozen worker profiles. Do not claim multiple native parser configurations.

Initial experimental caps: 5 GiB Activity container memory, 4 CPUs, 2 GiB scratch,
100 MiB input/original bytes, 51 processed pages, 20 million page pixels.
These are experimental rejection settings; supported maxima come from measurements.
Retain startup 120s, native no-progress 180s, child hard 540s, preflight 30s;
12min Activity/40min total, heartbeat 15s, 3 attempts, retry 2–10s;
30s SDK drain, 5s TERM + 5s reap, 60s Pod grace.
Recycle after 20 successful groups to exercise a bound within this matrix.

## Run sequence and acceptance

First complete each primary workload in a fresh worker, then sequentially process
WikiSkill → YOLO26 → AIMA → 51-page paper → WikiSkill with new captured versions
to force actual inference and expose cross-document state. Compare entire typed
Docling JSON against fresh complete execution, with no text normalization.
Check request/source/result hashes, every durable OCR/evidence dependency,
canonical_accepted=false, graph/furniture/PictureItem children, actual types,
provenance and captions. Fresh equality is fidelity, not source-quality approval.

Carry forward S2 reviewed table cells for WikiSkill/YOLO, six ACL equations
(including TextItem eq2), ACL reading order, region-specific representation
uncertainty, AIMA code/caption evidence and Keynote's 27 textboxes. Use existing
source-audited expectations; never generate a quality oracle from tested output.
Review contiguous AIMA page joins separately; historical non-contiguous 87→104
association and universal AIMA order remain unqualified regardless of equality.
New full-paper content outside reviewed regions has execution/fidelity coverage,
not blanket semantic quality approval. No universal numeric quality threshold.

Exercise owned Activity Pod loss while the long paper has registered at least one
group and another group is active; then normal graceful deletion/drain during
long work. Record old/new UID, stage, incomplete work, retries, retained registration
identities, recovery duration and scratch/process cleanup. Re-submit accepted
requests after replacement and verify all intact registrations are reusable.

## Measurement and coordination

Every inference, rollout/fault experiment and measurement holds a live host
Python fcntl.flock on /private/tmp/data-ingestion-pdf-qualification.lock for the
entire run, released in finally. Nonblocking acquisition reports waiting; coding
and fixture preparation continue without holding it. Namespace pdf-t09a-validation,
bucket t09a, dedicated queues/prefixes only. Never alter T06/T07/T08 resources.

Sample native child RSS plus whole single-container Pod cgroup memory.current,
memory.peak/events, CPU, scratch logical/allocated bytes and free storage throughout
all stages, including assembly/OCR/finalization; retain timestamped series and
sampling gaps. Record node, architecture/image ID, model/profile/producer hashes,
resource settings, restarts and parser recycles. Inventory competing cluster/host
load before/during trials. Contaminated trials are excluded from resource acceptance.
Unknown bounds, untested deadline exhaustion and production storage/HA remain gates.
T09b owns tuning; T10 consumes this bounded evidence. No push/merge/ticket closure.

## Source review supplement (before clean fresh scoring)

Visual inspection of AIMA originals 101/103/104/107/108/110 confirms the selected
context contains CodeItems expected for breadth-first (Figure 3.11), uniform-cost
(3.14), depth-limited (3.17), iterative-deepening (3.18), plus diagram/table captions.
The sentence at the bottom of original 103 continues with “to Bucharest with cost”
at the top of 104. Check that particular contiguous join and its two source regions;
do not infer universal marginalia association/order from it. Exact algorithm symbols,
full new table mathematical text and all section-wide order remain source-review
gates beyond these declared relationships. Existing S2 page-104 representation
uncertainty must remain region-bound and readable.

The first trial encountered API/monitoring timeouts and is excluded from resource
acceptance. A controlled recovery demonstrated a continuous window but is only
partial-workload observation. Clean fresh trials must have complete monitoring;
report all gaps and API/pressure events. In addition to flock, require explicit
peer quiescence after any controller failure; lock availability alone cannot certify
that a disconnected remote process has stopped.

For the four new AIMA algorithm blocks, preserve a private source-native transcript
and commit only its hash. Compare the complete sequence after whitespace-only
removal (layout/indentation and executable semantics are not being qualified).
Do not normalize non-whitespace symbols. The PDF native layer emits U+0007 before
`=` on originals 107/108 where the visual source shows inequality. Record that
source-font limitation explicitly; equality to a native transcript does not qualify
literal mathematical/code representation. Full fresh/warm JSON comparisons still
use no normalization. Any sequence difference is a recorded quality gate.
