# S2 bounded content validation — 2026-09-13

**Verdict: supported candidate strategy with unresolved symbol-representation qualification; #35 remains open and remains a blocker of #41.** This is throwaway research, not production implementation or universal PDF support. No new numeric acceptance threshold was introduced. T06 durable delivery is a separate integration gate, not the reason S2 remains open.

## Execution and provenance

Starting research commit: `c5c35ef6b743f3f98aee4925b79814d6f8fa004d`. Existing evidence was audited without rerunning its inference. New inference was exactly ACL physical pages 2–4 (printed 5544–5546) and one Keynote-export PDF page. Source identities, byte sizes, revisions, physical page mappings, exact TOPLEFT point rectangles, text-region hashes and partial orders were frozen in [new-plan.json](new-plan.json) before execution. The plan hash was `16bd5eb410f2419efbb91218c3ae467aa98dc6096c90a9ddedd633dbe4966442`; its independent local source oracle hash is recorded there. All source native glyphs were assigned to the predeclared region inventory. This is not proof that native glyph extraction is a perfect gold: font-encoding ambiguities were retained for visual review.

[run.sh](run.sh) runs the four-page experiment from the original source directory. [new-environment.json](new-environment.json) pins Linux, packages, options, producer and all 17 matching baseline model artifacts. Docker image: `sha256:8ffaac39462e87d281274f92e4fa290aa905a054d40692646f1d3d42490f1ee0`; network disabled, CPU 4, 5 GiB limit. Native OCR and formula enrichment were disabled; table structure and page/picture images enabled, image scale 1. No Temporal/Kubernetes workflow or production adapter was exercised.

Full source strings, PDFs, raw Docling documents, images and OCR text remain local and ignored. EVIDENCE.json binds those artifacts by hash without uploading copyrighted content. Source crops are research controls, not demonstrated durable delivery.

## Per-target and per-check results

| Target/check | Result | Evidence and limits |
|---|---|---|
| WikiSkill physical 5, complete table | **Supported** | 26 rows × 8 columns, five row-span-5 cells, 188 unique cells. Every full cell matches source after whitespace-only comparison: 188/188. Source column boundaries were corrected where initial oracle rectangles included adjacent header characters; parser output was unchanged. |
| YOLO physical 5, complete table | **Supported with explicit source-font disposition** | 15 × 4, all 60 cells audited. Raw exact count remains 45/60. Remaining 15 differ only by PDF-font U+0002 versus the visible literal hyphen encoded as `-` by Docling; no other word/value difference. This source-specific font decoding is not a global U+0002 rule. Geometry/spans retain their prior checks. |
| AIMA physical 87 / printed 68, full selected paragraph | **Supported** | Complete paragraph whitespace-exact against corrected CropBox-aware source extraction. The historical incorrect “elements of a problem” anchor remains excluded; actual source says “elements define a problem”. |
| AIMA physical 104 / printed 85, full uniform-cost paragraph | **Content preserved; representation disposition required** | No word omission after CropBox correction. Source em dash becomes hyphen in prose; source-font U+0002 becomes epsilon. The epsilon is a source-specific encoding interpretation, not interchangeable with the YOLO hyphen. Original comparison remains non-exact. |
| AIMA physical 86 / printed 67, entire CodeItem | **Supported as source-backed algorithm content** | `#/texts/11`, caption `#/texts/12`: complete instruction sequence whitespace-exact. Both nested `if` branches, null-action return, action selection, sequence update and final return are present. Source grouping distinguishes persistent declarations, outer empty-sequence branch and inner failure branch. Flat text does not retain executable indentation/AST; preserve CodeItem type, caption and readable source rather than coerce to PictureItem or claim executable code reconstruction. |
| AIMA footnotes/margin note | **Supported with explicit association uncertainty** | Footnotes 6/7 remain separate items on physical 104 (`#/texts/12`, `#/texts/13`), with body markers and source provenance. The DEPTH-FIRST SEARCH margin note (`#/texts/3`) remains separate. No typed crosslink proves association; marker/page or nearby-heading association is source-reviewed, and machine association remains explicitly uncertain. Do not merge into prose. |
| ACL physical 2–4, all selected prose regions | **Completeness supported; meaningful-symbol representation unresolved** | Full-region comparisons identify discretionary source-font line-end hyphens, structural list bullets, one cross-column paragraph continuation merged coherently into the preceding item, and curly-apostrophe conversion. No missing prose word was found. Physical 4 also changes mathematical minus `−` to `-` in the learning-rate exponent and reorders the overbar relative to `u`. These are explicit unresolved semantic representation cases, not whitespace matches or blanket equivalents. |
| ACL reading order | **Supported for selected body/caption paths** | Left-column flow continues into right column; the physical-3 continuation is inside the same TextItem, so requiring a new item or disjoint indices was invalid. Cross-column Figures 1/2 and their captions remain connected. Figure 1 native symbol sequence differs from PDF glyph sequence; a diagram does not require an arbitrary total textual linearization. Preserve spatial source. |
| ACL figures/captions | **Supported source-backed candidate** | Two PictureItems with caption references and native child texts. Figure 2 full native figure text is exact; Figure 1 differences are ordering of `+` and alpha across diagram positions. Full graph traversal includes children; default iterator/Markdown alone does not establish their loss or completeness. |
| ACL equations 1–6 | **Occurrence and readable source supported; typed classification exception explicit** | All six occurrences have result-local identity and readable actual detected-region crops; [formula-evidence.json](formula-evidence.json) records exact refs, page, region and hashes. Equation 2 is `TextItem #/texts/55`, not FormulaItem. Preserve that actual type; formula inventory cannot depend solely on the FormulaItem label. Equation 5's bbox includes a little adjacent prose; formula itself is complete. No LaTeX recognition or mathematical interpretation claimed. |
| Keynote-export page 1, full textboxes | **Supported: 27/27 whitespace-exact** | Recursive body + furniture + PictureItem children audit finds all complete source textboxes, including the common-note ending. This is a single PDF slide, not native PPTX, animation or speaker notes. |
| Slide diagram/partial reading order | **Supported as source-backed diagram; no graph extraction claim** | Node labels remain separate typed children under one PictureItem. Top knowledge flow, feedback labels, and the two independently readable panels preserve source positions. No arbitrary unique order is imposed between panels. Arrows are readable in retained source pixels; explicit graph-edge/association extraction remains unclaimed. |
| Prior Traditional Chinese manuals | **Supplemental evidence unchanged** | Reuse native and screenshot results in ../real-2026-09-13 and ../native-2026-09-13. Historical original 15/16 figure OCR score remains; “Skil Proposer” is accepted nonblocking only for that component. The invalid ground-truth probe stays excluded. No new TC inference or broad language coverage claim. |

## Harness corrections and excluded interpretations

- `new-results.json` used an enum/string coordinate comparison incorrectly. Its all-zero region results are invalid scorer output, not parsing failures. `corrected-new-results.json` fixes coordinates but still uses default traversal and exclusive region allocation; it is diagnostic only, not the final acceptance score.
- [graph_audit.py](graph_audit.py) traverses the complete typed graph and furniture. [graph-audit-summary.json](graph-audit-summary.json) reports overlap candidates and literal comparisons; shared cross-column paragraphs require within-item source review, not automatic failure from duplicated overlap.
- Initial AIMA source audits forgot nonzero CropBox origins and clipped words. Corrected source audits add the PDF CropBox offsets. Physical 86/104 origin is approximately (46.80, 57.60), physical 87 (21.60, 57.60), in PDF points. Their earlier clipped-source comparisons are excluded, not parser regressions.
- A truncated inspection display suggested the slide lost `consistency.`. Complete raw JSON disproves this: `#/texts/37` contains the full sentence. One already-announced 3× actual-PictureItem crop OCR control was executed before that correction; it recovered the whole sentence. [slide-crop-control.json](slide-crop-control.json) is a redundant diagnostic and **does not demonstrate repair of a real missing word**. No further OCR or inference followed.

## Minimal remaining work and gate ownership

1. Resolve the concrete ACL physical-4 minus/overbar and AIMA punctuation/source-font representation cases under the accepted rule that meaningful symbols must not be silently normalized. The current parsed `orig` is not an independent verbatim source transcript. A source-backed explicit uncertainty/representation record is a candidate remedy; approving it as sufficient versus requiring text correction remains a content-contract decision. Do not call these exact or universal equivalents.
2. Confirm consumers traverse typed picture children and furniture and preserve TextItem-classified formula occurrences, complete CodeItem/caption/source relationships, and explicit note-association uncertainty. Static evidence establishes why this contract is necessary. Production enforcement and resolvable durable evidence belong to T04/T06 acceptance, not another broad S2 benchmark.
3. No further executable S2 question is currently required before those concrete representation decisions. If the chosen remedy changes extraction behavior, a narrowly targeted regression on these already selected regions can answer it. No model search, rescanning matrix or whole-book test is proposed.

#35 stays open for item 1; #41 is not unblocked in this round. This does not invalidate the native OCR-off + selective component enrichment direction. It prevents content-quality approval from silently accepting semantic text changes. Scanned/historical/faulty-layer repair remains deferred, not passed; unsampled languages and native PPTX remain unqualified. Future LaTeX and future required enrichment still need durable success before `complete`, outside this experiment.
